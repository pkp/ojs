@if ($section)
    <x-metadata.default
        slug="section"
        title="{{ __('section.section') }}"
        :$headingTag
    >
        {{ $section->getLocalizedTitle() }}
    </x-metadata.default>
@endif
