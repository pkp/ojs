@if ($section)
    <x-metadata-blocks.default
        slug="section"
        title="{{ __('section.section') }}"
        :$headingTag
    >
        {{ $section->getLocalizedTitle() }}
    </x-metadata-blocks.default>
@endif
