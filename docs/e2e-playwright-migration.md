# E2E Playwright migration — roadmap

This is the shared roadmap for migrating the Cypress suite to Playwright as focused, feature-based specs. Each subsequent migration PR picks a row from §1, writes the spec, and ticks the row off. Endpoint extensions in §2 unblock later waves.

## Context

The main reason we're refactoring e2e coverage (in addition to moving to Playwright) is that today's Cypress suite is one long serial chain. A failure at spec #25 forces rerunning 1..24 first; a bad debug cycle is 30–40 min. That's a consequence of the fixture model: ~20 files under `cypress/tests/data/60-content/` named `*Submission.cy.js` double as test **fixtures** (create a submission, leave it on disk for later specs) AND as test **bodies** (embed editorial-workflow assertions mid-creation). Integration specs downstream then call `findSubmissionAsEditor('dbarnes', null, 'Corino')` to find "their" submission — a hard dependency on the serial order above having run first.

The Discussion Manager migration shipped on `e2e_revamp` demonstrates the alternative: each test calls `pkpApi.createSubmission(spec)` with a unique tag and owns its state end-to-end. No cross-spec dependencies, parallel workers work, a failed test blocks only itself.

## Principles

1. **One feature = one spec.** A feature is the unit of organization, not the unit of testing. A single spec can — and often should — contain many `test()` blocks covering the feature's distinct UI surfaces, roles, and edge cases. The benefit Playwright brings over Cypress is **test independence** (each test seeds its own state, parallel-safe), not necessarily fewer tests. Consolidating Cypress's redundant serial fixtures into one self-contained test is correct; *dropping* a UI surface that Cypress covered is not. Split into multiple specs only when one spec would become genuinely unwieldy (rough threshold: more than ~10 tests on visibly distinct surfaces) — and then split by surface, not by capability.
2. **Surface coverage parity with Cypress is the baseline.** Every distinct UI surface, role, screen, and assertion that the Cypress source exercises must remain covered after migration. The migrated spec may have far fewer `test()` blocks (Cypress's serial chain frequently re-exercises the same surface across files), but no surface that Cypress covered may disappear without an explicit, recorded reason in the row. Valid drop reasons: the assertion duplicates an existing unit test; the flow is genuinely obsolete; the feature has been removed from the product. **Not valid:** "the capability is also asserted via the API" — UI surfaces have their own bugs (wiring, focus, async, validation messages, role gating) that an API round-trip misses.
3. **Each test seeds its own state.** Use `pkpApi.createSubmission(scenario)` per test; never rely on state left by another spec. Unique tags (worker-index + suffix) keep parallel runs from colliding.
4. **The bootstrapped journal is read-only for tests.** Bootstrap seeds a stable `publicknowledge` journal that every spec can *read from* (default sections, default email templates, default plugins off, etc.). Any test that needs to *mutate* journal-level configuration — creating sections, enabling Categories/Data-availability, adding navigation items, configuring a plugin (DOI, Crossref, Pubmed, ORCID), customising reviewer recommendations, creating task templates, changing wizard field config, etc. — must create its own scratch journal via extension **E0** (see §2). Same reason submissions are per-test: shared mutable state is what made Cypress fragile.
5. **Honor the shared / OJS-root split.** Behavior identical in OJS + OMP + OPS → `lib/pkp/playwright/tests/`. OJS-only concepts (issues, galleys, subscriptions, journal homepage, DOI, Pubmed, Crossref, native XML issue export) → `playwright/tests/`. POMs for shared UI components live under `lib/pkp/playwright/pages/` regardless of where the spec lives (the Discussion Manager precedent).
6. **Retire the submission-fixture specs entirely.** The 20 files under `cypress/tests/data/60-content/` have no feature value that isn't covered by more-specific specs elsewhere; their "data" value becomes per-test scenario seeding.
7. **Scenario endpoint grows only when needed.** Extend `PKPSubmissionScenarioController` lazily, per the first feature that needs a capability. Extensions are consolidated in §2.

## Organization: migration waves

Features are **not** grouped by UI area — most features span several surfaces, so area-grouping would be arbitrary. Each feature sits in exactly one **migration wave**, derived from a single objective question: *what's the most advanced scenario state the spec needs?*

| Wave | Enabling condition |
|---|---|
| 1 | No submission state — admin/config screens (may span admin + public page view, as long as no submission is needed). |
| 2 | Submission wizard — the test drives a fresh wizard run; no scenario seeding. |
| 3 | Draft / in-review / accepted submission — pre-publication editorial workflow. |
| 4 | Published content — publication lifecycle; each spec freely mixes editor actions and reader-side verification of the same capability. |
| 5 | Participants + permissions — role-specific scenarios that assert access control. |
| 6 | Admin cross-cutting — multi-journal, impersonation, jobs, API smoke. |
| 7 | Blocked — requires a scenario-endpoint extension (E1–E6 in §2). |
| 8 | Retire Cypress — delete the old suite once every row above is green. |

The wave also gives execution order: start at row 1, work down.

## §1 · Features by wave

**Flat path.** Every spec lives at `{Home}/playwright/tests/{Spec file}` — `Home = lib/pkp/` for shared specs, empty for OJS-only. **No subfolder taxonomy is predecided**; folders can emerge later as an organic refactor once ~15–20 specs are written and natural clusters are obvious. That avoids the "is Categories a settings/ thing or a submission-wizard/ thing?" trap that UI-area folders keep falling into.

Columns in the wave tables:
- **Home** — `lib/pkp` = shared · `ojs` = OJS-only.
- **Spec file** — filename inside `{Home}/playwright/tests/`.
- **Cypress source** — spec(s) contributing the coverage.
- **Tests** — concrete one-liners. `E:` = editor/admin tests · `R:` = reader tests · both may appear in one spec.
- **Ext?** — `—` if none needed; otherwise references §2.

### Wave 1 · No submission state

| # | Feature | Home | Spec file | Cypress source | Tests | Ext? |
|---|---|---|---|---|---|---|
| 1 | Announcements CRUD ✅ DONE | lib/pkp | `announcements.spec.js` | `lib/pkp/.../Announcements.cy.js` | E: four tests on E0 scratch journals — (a) create (TinyMCE body) + edit + delete via the listPanel admin; (b) toggle `enableAnnouncements` via Website Settings → Setup → Announcements sub-tab + Save (race with context PUT) + reload + assert the nav-item appears, then untick + Save + reload + assert it disappears; (c) reader-side `/announcement` listing page renders the seeded announcement title for an anonymous visitor on a journal with `enableAnnouncements=true`; (d) `/{journal}/sitemap` returns XML and contains a `<loc>` mentioning "announcement". `ContextBuilderProcessor` extended with an `enableAnnouncements` scalar passthrough for tests (c) + (d) so the reader/sitemap routes are wired without driving the toggle UI again. | **E0** |
| 2 | Navigation menus ✅ DONE | lib/pkp | `navigation-menus.spec.js` | `lib/pkp/.../NavigationMenus.cy.js` | E: (a) on E0 scratch journal, dbarnes opens Setup → Navigation; clicks `a.pkp_linkaction_addNavigationMenu` to open the modern Vue `NavigationMenuManagerFormModal` (`[data-cy="active-modal"]` + embedded `[data-cy="navigation-menu-editor"]`); asserts assigned panel is empty + unassigned panel has >4 items including Register / Login; saves a new menu, edits its title via the row's title-link (re-opens the same Vue modal in edit mode), tests duplicate-title validation ("This title already exists…" stays in the modal), tests area-conflict validation against the default Primary Navigation Menu installed by `NavigationMenuDAO::installSettings`; cancels validation-failed modals through the `[data-cy="dialog"]` Yes/No unsaved-changes prompt; deletes the menu via the row's `a.show_extras` → Remove → OK confirmation. (b) on a separate E0 scratch journal, exercises the legacy items grid (still pure jQuery `AjaxModal` + fbv `form#navigationMenuItemsForm`): adds a `NMI_TYPE_CUSTOM` item with localized title + path; edits the title via row controls; deletes via row controls. The form-handler split (Vue modal for the menu editor; legacy AjaxModal for the items) tracks the live mid-migration UI surface and is the reason the row was deferred — keeping coverage on both sides confirms the link-action contract still hands off correctly to either pipeline. | **E0** |
| 3 | Editorial masthead ✅ DONE | lib/pkp | `editorial-masthead.spec.js` | `lib/pkp/.../EditorialMasthead.cy.js` | R: masthead page renders for anonymous readers | — |
| 4 | Email templates ✅ DONE | lib/pkp | `email-templates.spec.js` | `lib/pkp/.../emailTemplates/EmailTemplates.cy.js` + `cypress/.../emailTemplates/EmailTemplates.cy.js` | E: toggle default template unrestricted→restricted with user-group assignment; create restricted custom template with TinyMCE body + two user groups; create unrestricted custom template and verify user-group checkboxes are hidden — and within the same session flip the radio back to restricted to verify the user-group checkboxes reactively reappear (Cypress test 9 show-direction); create a restricted custom template with zero user groups and confirm the form persists the no-UG state across reload (Cypress test 7). Cypress's bidirectional toggle pair (mark unrestricted → mark restricted) folds into the single round-trip in test 1 — direction, not surface. | **E0** |
| 5 | Multilingual form fields ✅ DONE | lib/pkp | `multilingual.spec.js` | `lib/pkp/.../Multilingual.cy.js` | E: toggle fr_CA UI-active flag from the Languages grid (round-trip via reload); enter French in the masthead acronym field when the UI locale is disabled and verify the value persists across reload; on an in-review submission seeded into a fr_CA-enabled scratch journal, switch the publication's Title & Abstract panel to French and assert the FR title round-trips via REST (mirrors Cypress step 3). | **E0** |
| 6 | Reviewer-recommendation customisation ✅ DONE | lib/pkp | `reviewer-recommendations.spec.js` | `ReviewerRecommendation.cy.js` | E: defaults render + have type; CRUD custom recommendation; active/inactive toggle; in-use recommendation locks Edit/Delete (DropdownActions hidden via `v-if="item.removable"` once a review_assignment row references it); inactive recommendation does not appear in the reviewer Step 3 dropdown (`Repo::reviewerRecommendation()->getRecommendationOptions` filters by ACTIVE). The two in-review-dependent tests landed once `submission-in-review.js` accepted a `journal` override and `ParticipantProcessor` reconciled flags onto pre-existing stage assignments. | **E0** |
| 7 | Issues ✅ DONE | ojs | `issues.spec.js` | `cypress/.../50-CreateIssues.cy.js` | E: create future issue; edit volume/number/year (value persists on reload); publish; set-current swaps between two published issues; unpublish moves the issue back to Future · R: anonymous archive page lists the published issue and its public view page loads · M: publishing with `sendIssueNotification` ticked dispatches the IssuePublishedNotify email — Mailpit `latestTo(dbarnes@mailinator.com)` shows subject "Just published: Vol. 5 No. 1 (2025) of …" and the body references the issue identification. dbarnes is both manager and recipient because `NotificationSubscriptionSettingsDAO::getSubscribedUserIds` keys off active user_user_groups rows in the journal context (no separate reader user needed); jobs run in `PKPQueueProvider`'s `register_shutdown_function` so the email lands within Mailpit's 10s poll window. Reader "TOC renders with section grouping" reduced to "view page loads + shows Vol/No/Year heading" — a seeded TOC assertion requires issue-assigned published articles (row #30) and galleys (row #51). | **E0** |
| 8 | Sections ✅ DONE | ojs | `sections.spec.js` | `cypress/.../50-CreateSections.cy.js` | E: create new section; edit-inactive flag persists across reload; edit editor-only flag persists across reload; configure section fields (wordCount + identifyType + abstractsNotRequired) and verify persistence both via re-opening the form and via REST `/api/v1/sections` (anchored on `name=` selectors since fbv collapses the multilingual id to `identifyType-{uniqId}` when the scratch journal supports a single locale); assign multiple section editors (dbarnes as `editor`, dbuskins + minoue as `sectionEditor`) to the Articles section and verify the three checkboxes are still ticked on reload. dbarnes needs the `editor` user-group rather than bare `manager` because PKPSectionForm filters `assignableUserGroups` by `withStageIds(WORKFLOW_STAGE_ID_SUBMISSION)` and the manager group has no stages in registry/userGroups.xml. Wizard-side effect of editor-only sections deferred to row #12 (Wizard — section rules) to keep this spec scoped to admin-UI. | **E0** |
| 9 | Subscription types & policies ✅ DONE | ojs | `subscription-config.spec.js` | `Subscriptions.cy.js` (first half) | E: five tests on E0 scratch journals — (1) create subscription type (name + CAD + cost + duration + individual); (2) edit subscription-policies form (contact name / email / mailing address) and verify persistence on reload; (3) delete subscription type via legacy `show_extras` → Delete → reka-ui OK dialog; (4) Distribution → Payments tab UI: tick Enable + select Manual Fee Payment plugin + Canadian Dollar currency + fill manualInstructions, save (race with the `_payments` API PUT — note this form posts to `/api/v1/{ctx}/_payments`, NOT the generic context endpoint), reload + reactivate the tab + assert all four fields persisted; (5) Distribution → Access publishingMode toggle: pick "The journal will require subscriptions" radio, save, reload, assert `publishingMode=1` (PUBLISHING_MODE_SUBSCRIPTION) persisted both in the radio state and in the context REST payload. Per-issue access-status config dropped — it's tightly bound to the reader-side gate already exercised end-to-end by row #52 via the `accessStatus` passthrough. Reader-side access tests stay in row #52 (E4 territory). | **E0** |
| 58 | Public user registration ✅ DONE | lib/pkp | `user-registration.spec.js` | `lib/pkp/cypress/support/commands.js` (`cy.register` helper) + `cypress/.../60-content/AmwandengaSubmission.cy.js` (`Registers as author and creates a submission`) | R→E: anonymous `browser.newContext({reducedMotion:'reduce'})` (no storageState) navigates to `/publicknowledge/user/register`, fills the FBV-free Smarty form anchored on stable `name=` selectors (givenName / familyName / affiliation / `select[name="country"]` by ISO alpha-2 = `CA` / email / unique-tag username `reg-w${parallelIndex}-${suffix}` / password×2 / `privacyConsent`), submits and waits for the redirect chain to settle on `/user/register` (RegistrationHandler logs the user in via `Validation::login` then `redirect(null,'user','register')`, which then renders `userRegisterComplete.tpl` because `Validation::isLoggedIn()` is true). Asserts the "Registration complete" heading, clicks the "Make a New Submission" CTA (`<li class="new_submission">`), asserts the wizard's Start step renders via the "Make a Submission" heading + the StartSubmission Vue form's TinyMCE iframe `#startSubmission-title-control_ifr` + the Section FieldOptions wrapper showing the bootstrap "Articles" option label. Doesn't drive the wizard further — Wave 2 (#10–#17) covers wizard mechanics. Dropped (scope) the optional log-out → log-in-with-new-credentials variant — the registration handler already logs the user in mid-flow, so the post-submit landing page is itself the end-to-end "registration creates a usable account" assertion. Closes the Cypress `cy.register → 'Make a New Submission' → wizard` pattern bundled into ~20 `60-content/*Submission.cy.js` specs without needing a separate row for the post-registration handoff. | — |

### Wave 2 · Submission wizard

Each test runs a fresh wizard session; the wizard writes through its normal API.

| # | Feature | Home | Spec file | Cypress source | Tests | Ext? |
|---|---|---|---|---|---|---|
| 10 | Wizard — validation ✅ DONE | lib/pkp | `wizard-validation.spec.js` | `SubmissionWizard.cy.js` tests 4–5 | E: author clears Title, advances to Review, verifies errors banner + Title "This field is required." + file-missing warning + Submit disabled; re-opens Details, restores Title, returns to Review, verifies Title error clears. Full happy-path submit deferred — requires uploading an Article Text file (row #17's territory) since the no-file warning stays gating Submit. | — |
| 11 | Wizard — copyright gate ✅ DONE | lib/pkp | `wizard-copyright-notice.spec.js` | `SubmissionWizard.cy.js` test 3 | E: E0 scratch journal seeded with `copyrightNotice`; author runs the wizard end-to-end, verifies the notice text renders inside a `<blockquote>` in the Confirmation panel, toggles the `confirmCopyright` checkbox across unchecked→checked→unchecked and verifies Submit remains disabled (compound gate with the file-missing error). FR-locale variant: scratch journal supports `['en', 'fr_CA']` with bilingual `copyrightNotice`; session UI flips to fr_CA via `/index/user/setLocale/fr_CA`; the FR-rendered Confirmation panel's blockquote contains the fr_CA copyright text (and the en string does NOT leak through), and the FR Submit ("Soumettre") button gate works the same. Submit-success deferred for the same file-upload reason as row #10. `ContextBuilderProcessor` extended to accept `copyrightNotice` as an optional passthrough so the journal is immutable-after-create. | **E0** (extended) |
| 12 | Wizard — section rules ✅ DONE | lib/pkp | `wizard-section-rules.spec.js` | `SubmissionWizard.cy.js` test 2 | E: two tests on E0 scratch journals — (a) inactive section: dbarnes=manager creates a Reviews section, asserts both labels render in the Start form's section radio, marks Articles inactive via the legacy sections grid, verifies the section FieldOptions wrapper + the Articles option both disappear (getSubmitSections's `->excludeInactive()` filter); (b) editor-restricted section: dbarnes=manager + atester=author on the scratch journal, dbarnes creates Reviews + marks Articles editorRestricted via the same legacy grid (new `markSectionEditorRestricted` helper mirrors `markSectionInactive`), atester logs in and opens the wizard, asserts Articles is filtered out of the Start form. atester is the baseline author user added for row #43; enrolling her on the scratch journal as an author satisfies the editorRestricted gate's non-editor precondition. | **E0** |
| 13 | Wizard — language change ✅ DONE | lib/pkp | `wizard-language.spec.js` | `SubmissionWizard.cy.js` test 5 | E: start wizard in English + Articles; open "Change Submission Settings"; pick French (Canada) + Reviews; save; verify caption re-renders and the Details step mounts with `titleAbstract-title-control-fr_CA` as the active locale control (French title value round-trips through editor.save()). Second test exercises the FR-locale review-panel error rendering: with the submission's primary form locale flipped to fr_CA the Title field is cleared and the wizard advanced to Review, asserting the per-panel heading reads "Details (French (Canada))" with the localized "This field is required." error and the corresponding EN review panel carries no equivalent error. Dropped the journal-field-config mutation (row #16's scope) and the file upload + "Submission complete" assertion (row #17). The English-alongside-French secondary-locale reveal is covered by row #5's multilingual spec; the categories-render-in-UI-language assertion is covered by row #15's wizard category exposure test. Extends `SubmissionWizardPage` with `openReconfigureModal` / `changeReconfigureSettings` / `setCommentsForEditors` / `currentSubmissionId`. | — |
| 14 | Wizard — comments → discussion ✅ DONE | lib/pkp | `wizard-comments-become-discussion.spec.js`, `scenario-default-file.spec.js` | `SubmissionWizard.cy.js` test 1 | E: three layers of coverage. (a) wizard-comments-become-discussion test 1 drives the live wizard up to Review and asserts the comment renders in the "For the Editors" review panel (autosave + review-panel binding); (b) test 2 (new) drives the wizard end-to-end — Reviews section, sets the publication title on Details, uploads `default-article.pdf` via the wizard's plupload UI (`setInputFiles` on the hidden file input, race the upload POST, click the Article Text genre button — same pattern row #17 / row #48 use), types the comment on For-the-Editors, advances to Review, clicks Submit, confirms the modal, waits for the "Submission complete" heading. Then verifies the workflow page renders the Discussion Manager with a "Comments for the Editor" entry whose detail-modal body contains the comment. (c) scenario-default-file's second test seeds a submission with `commentsForEditor` + `submitted: true` and asserts the same Stage 1 discussion server-side — the back-end pathway proven independently. The scenario processor mirrors the wizard end-to-end: every seeded submission gets a default Article Text file via the same `Repo::submissionFile()->add()` call as `SubmissionFilesUploadForm::execute()`, and `Repo::submission()->submit()` fires when `submitted: true`. Substituted `dbarnes` for Cypress's `ccorino` (not in Playwright baseline). | **E1** (default file + submit + commentsForEditor) |
| 15 | Categories ✅ DONE (partial) | lib/pkp | `categories.spec.js` | `lib/pkp/.../Categories.cy.js`, `cypress/.../50-CreateCategories.cy.js` | E: (a) E0 scratch journal (no `submitWithCategories`) — run the wizard to "For the Editors" + Review, verify no "Select Categories" button, no `forTheEditors-categoryIds-*` label, and no "Categories" review-panel header; (b) E0 scratch journal seeded with `submitWithCategories: true` (ContextBuilderProcessor passthrough, new scalar alongside `copyrightNotice`) — create one category via the Categories admin UI, run the wizard, open the "Select Categories" picker, tick the category, Save; advance to Review and assert the "Categories" review-panel item renders the selected label; (c) **graduate**: nested-hierarchy creation (parent → child → grandchild) + a sibling under the same parent via the Categories admin's "More Actions → Add" menu (no parent dropdown — `categoryManagerStore.getCategoryForm` routes the form action through `?parentCategoryId={id}`), then run the wizard and pick the grandchild; assert the wizard's Review panel renders the full breadcrumb `Applied Science > Computer Science > Computer Vision` (`Repo::category()->getBreadcrumbs` + `common.categorySeparator` template) and that the sibling does NOT leak into the selection. **Dropped**: end-to-end Submit + DB round-trip on the selection (same file-upload gate as rows #10/#11/#14 — reopens with E1); the UI-side settings-flip path (seeded server-side to keep the journal immutable-after-create); category-management CRUD / edit / delete (not part of the wizard feature). Substituted `dbarnes` (baseline manager) for Cypress's `catauthor` (the feature is role-agnostic; see `ForTheEditors::addCategoryField`). | **E0** (extended) |
| 16 | Wizard — field-config reset ✅ DONE | ojs | `wizard-config-reset.spec.js` | `SubmissionWizard.cy.js` test 6 | E: on E0 scratch journals, exercise the FieldMetadataSetting wiring across both wizard host forms via four focused tests — (a) flip `keywords` to Require, assert Keywords label in Details carries `.pkpFormFieldLabel__required`; (b) flip `keywords` to Do-not-ask (METADATA_DISABLE), assert the Keywords control is absent from Details; (c) flip `subjects` to Require, assert the Subjects label inside the ForTheEditors step (Step 4) carries the required-asterisk marker — proves the wiring is general across the two host forms (titleAbstract for keywords, forTheEditors for subjects); (d) flip `keywords` to Require, walk to Review without supplying a keyword, assert the wizard's top-level errors banner appears and the Details review panel surfaces the localized "This field is required." string scoped to the Keywords entry. Dropped the rest of Cypress test 6's 10-field flag-flip preamble (every field shares the same FieldMetadataSetting + context schema gate). Also dropped the Cypress "reset to defaults" assertion — E0 per-test scratch journals make the reset a tautology. | **E0** |
| 17 | Filenames sanitization ✅ DONE | lib/pkp | `filenames.spec.js` | `lib/pkp/.../Filenames.cy.js` | E: on E0 scratch journal, start a fresh wizard as dbarnes, upload a PDF whose on-disk name contains `Édition £ 丹尼爾 & دانيال (tag).pdf` (non-ASCII + punctuation + parentheses + spaces), assert the upload POST's response records `name.en` identical to the uploaded filename, assert the listPanel item DOM shows the filename, pick the first primary genre, assert the filename is still intact after the re-render, then GET the legacy `$$$call$$$/api/file/file-api/download-file?submissionFileId=…&submissionId=…&stageId=1` endpoint and assert the response's `Content-Disposition` header carries `filename*=UTF-8''<encoded-name>` whose URL-decoded form equals the original uploaded filename (PKP's encoder uses form-style URL encoding — `+` for spaces — so spaces are normalised before `decodeURIComponent`). The "blocked on E1" claim was stale: the wizard's plupload upload itself satisfies the file-existence precondition the download endpoint needs — no scenario-side file seeding required. The multilingual filename round-trip stays out of scope (the wizard uploader only stores a single localized name; that round-trip belongs to `SubmissionFilesEditModal` coverage elsewhere). | — |

### Wave 3 · Draft / in-review / accepted (pre-publication workflow)

Scenario seeds the appropriate pre-publication state and the spec exercises workflow UI.

| # | Feature | Home | Spec file | Cypress source | Tests | Ext? |
|---|---|---|---|---|---|---|
| 18 | Discussion Manager ✅ DONE | ojs (spec) / lib/pkp (POM) | `discussion-manager.spec.js` *(currently at `discussions/discussion-manager.spec.js` — flatten during folder refactor)* | `Discussions.cy.js` | E: full CRUD + access control | — |
| 19 | Decision: send to review ✅ DONE | lib/pkp | `decision-send-to-review.spec.js` | embedded in `AmwandengaSubmission`, `CcorinoSubmission`, `DdioufSubmission` | E: seed stage-1 via `submissionDraft`; dbarnes clicks "Send for Review"; advance past the notifyAuthors email step (wait for the auto-loaded template — submitting with an empty subject/body 400s); Record Decision; assert the completion dialog reads "has been sent to the review stage"; verify via REST that submission.stageId flips to external-review and a DECISION_EXTERNAL_REVIEW row was written. `EditorialWorkflowPage` gained `clickDecision` / `clickContinue` / `recordDecision` / `viewSubmissionFromCompletionDialog` / `awaitEmailTemplateLoaded` / `fetchSubmission`. | — |
| 20 | Reviewer assignment (UI-level) ✅ DONE | lib/pkp | `reviewer-assignment.spec.js` | embedded in `AmwandengaSubmission`, `DdioufSubmission`, `DphillipsSubmission` | E: seed `submissionInReview({reviewers: []})`, dbarnes opens the Add Reviewer side-modal from the ReviewerManager panel, picks phudson via `Select Paul Hudson`, sets review method to Anonymous Reviewer/Disclosed Author (SUBMISSION_REVIEW_METHOD_ANONYMOUS=1), asserts the auto-populated response + review due dates are non-empty, submits; verifies the new assignment appears in the reviewer-manager DOM with the anonymity label + round-trips via the submission REST payload's `reviewAssignments` (one item, reviewer=Paul Hudson, reviewMethod=1). Dropped a distinct "change anonymity option" variant — the one submit exercises both the radio group and due-date form; splitting adds no coverage. | — |
| 21 | Decision: decline ✅ DONE | lib/pkp | `decision-decline.spec.js` | scattered in submission specs | E: (a) seed stage-1 via `submissionDraft`, click "Decline Submission" (InitialDecline / DECISION_INITIAL_DECLINE), single notifyAuthors step, Record Decision, assert submission.status = STATUS_DECLINED + decision row recorded; (b) seed an in-review submission via `submissionInReview` (two reviewers in invited/accepted states — neither completed, so Decline ships only the notifyAuthors step), click the review-stage "Decline Submission" (DECISION_DECLINE=6), same flow, same assertion. Reuses the `EditorialWorkflowPage` decision helpers added in row #19. | — |
| 22 | Section-editor recommendation ✅ DONE | lib/pkp | `section-editor-recommendation.spec.js` | `CcorinoSubmission.cy.js` test 3 | E: seed `submissionInReview` with minoue as a `recommendOnly: true` sectionEditor participant (`submission-in-review.js` fixture now accepts a `participants` override; `ParticipantProcessor` + scenario schema extended to honour per-participant `recommendOnly` / `canChangeMetadata` flags). As minoue, click "Recommend Accept", submit the single Notify Editors email step, assert the RecommendAccept decision (DECISION_RECOMMEND_ACCEPT=9) via the decisions API. As dbarnes, reopen the workflow and assert the review-stage secondary "Recommendation" panel renders "Accept Submission" (WorkflowRecommendOnlyListingRecommendations). | — |
| 23 | Stage-participant management ✅ DONE | lib/pkp | `stage-participants.spec.js` | `DdioufSubmission.cy.js` (copyeditor / layout editor / proofreader) | E: (a) seed a copyediting-stage submission via `submissionDraft` + `skipExternalReview` (stage=WORKFLOW_STAGE_ID_EDITING, participants=[dbarnes]), open the Participants panel's legacy addParticipant side-modal, filter the role <select> to Copyeditor, search "Fritz", tick Maria Fritz, submit OK, assert the list re-renders with "Maria Fritz / Copyeditor"; (b) seed the same copyediting-stage submission with mfritz pre-assigned, open her row's "More Actions" menu, click Remove, confirm the reka-ui PkpDialog ("Remove Participant"), assert Maria Fritz disappears from the panel while dbarnes remains. Dropped the role-filter / stage-scope variants from the roadmap — role selection is already covered by the add path, and the participant panel is shared identically across review/copyediting/production stages (same component, no stage-specific branches worth exercising). | — |
| 24 | Decision: accept ✅ DONE | lib/pkp | `decision-accept.spec.js` | `AmwandengaSubmission`, `DdioufSubmission` | E: (a) seed `submissionDraft`, dbarnes clicks "Accept and Skip Review" (SkipExternalReview, DECISION_SKIP_EXTERNAL_REVIEW=17), advance past notifyAuthors, Record Decision, assert stageId flips to WORKFLOW_STAGE_ID_EDITING=4 and the decision row is recorded; (b) seed `submissionInReview` (two reviewers in invited/accepted — no completed reviews, so Accept ships only notifyAuthors + promoteFilesToCopyediting), dbarnes clicks "Accept Submission" (Accept, DECISION_ACCEPT=2), same shape, same stage advance. Reuses the `EditorialWorkflowPage` decision helpers from row #19. | — |
| 25 | Task templates (config + apply) ✅ DONE | lib/pkp | `task-templates.spec.js` | `TaskTemplates.cy.js` | E: (a) dbarnes as manager on an E0 scratch journal adds a task template on the Copyediting stage via Workflow Settings → Task Templates tab (title + TinyMCE description via `setTinyMceContent('taskTemplate-description-control', …)` + `include` auto-add checkbox), saves through the `[data-cy="active-modal"]` side-modal, reloads the page, confirms the template row persists; (b) manager on a second E0 scratch journal seeds a Submission-stage task template (with `taskInfoAdd` + `dueInterval=P1W`), seeds a `submissionDraft`-shaped stage-1 submission in the same journal with dbarnes as editor participant, opens the workflow's Discussion Manager, clicks Add, selects the template button in the form's embedded `<ul role="list">`, asserts `input[name="title"]` pre-fills with the template title, `input[name="taskInfoAdd"]` is checked, `input[name="dateDue"]` is non-empty, and `tinymce.get('discussionForm-description-control').getContent({format: 'text'})` contains the template body; (c) form validation: submit the Add Template modal with empty title + description and assert "This field is required." appears at least twice; (d) management round-trip: create a template with `include=true`, verify the row's auto-add checkbox is checked, edit title via More Actions → Edit (confirm rename appears + original title gone), flip auto-add OFF via the row checkbox + Yes confirmation (assert checkbox unchecks), delete via More Actions → Delete + OK confirmation (assert row vanishes). Dropped (scope) the role-restrict visibility test (needs copyediting-stage seed + a restricted template + a non-copyeditor actor — defer until there's a reason to exercise the full visibility matrix). | **E0** |

### Wave 4 · Published content (editor + reader merged)

Scenario seeds a published submission (optionally with issue + versions + DOI). Each spec freely mixes editor actions and reader verification of the same capability.

| # | Feature | Home | Spec file | Cypress source | Tests | Ext? |
|---|---|---|---|---|---|---|
| 26 | Decision: send to production ✅ DONE | lib/pkp | `decision-send-to-production.spec.js` | `AmwandengaSubmission`, `DdioufSubmission` | E: seed a copyediting-stage submission via `submissionDraft` + a `skipExternalReview` scenario decision (stage=WORKFLOW_STAGE_ID_EDITING=4 without needing a review round). dbarnes clicks "Send To Production" (SendToProduction, DECISION_SEND_TO_PRODUCTION=7 — two steps: notifyAuthors + promoteFilesToProduction), advances past the auto-loaded email template, Record Decision, assert completion dialog reads "was sent to the production stage", verify via REST the submission.stageId flips to WORKFLOW_STAGE_ID_PRODUCTION=5 and a DECISION_SEND_TO_PRODUCTION row is recorded. Dropped the roadmap's "participants preserved" assertion — participant retention is already exercised end-to-end by the `stage-participants.spec.js` seeds and `submissionPublished` fixture (which runs the same decision as a prerequisite); adding it here would duplicate coverage without improving the decision-flow guarantee. Reuses the `EditorialWorkflowPage` decision helpers from row #19. | — |
| 27 | Publication metadata editing ✅ DONE (partial) | lib/pkp | `publication-metadata-editing.spec.js` | `AmwandengaSubmission.cy.js` | E: seed `submissionInReview({tag})` so the publication is still a draft. Two tests: (a) Title & Abstract + Metadata round-trip — dbarnes edits title + abstract via `setTinyMceContent`, types a keyword + Enter, saves each panel, asserts the `[role="status"]` "Saved" toast, then verifies the publication REST payload contains the new title/abstract/keyword and the workflow page's sub-header re-renders the title after reload; (b) Contributors panel — dbarnes opens Contributors, clicks Add Contributor, fills givenName + familyName + email + country + ticks the Author contributorRoles checkbox (required FieldOptions validator), saves, asserts the new contributor row renders in the list AND the publication's authors collection round-trips the contributor via REST. Galleys is exercised end-to-end by row #51. **Permissions and Issue panel save round-trips remain partially uncovered** — the Permissions panel's `optIntoEdit` gate (FieldText with Override-then-edit on copyrightHolder + copyrightYear) and the Issue panel's stacked-form Save targeting under the dashboard dialog turned out non-trivial to drive reliably from Playwright; the same field types and `pkpForm` save infrastructure are exercised across other rows (#11 wizard copyright, #16 wizard config, #28 publish-unpublish), so the field-specific bindings on those two panels are deferred. Dropped (scope) reader verification — published articles have an immutable published publication (editing surfaces `WorkflowPublicationEditWarning` and requires a new version), so the draft-publication path is the cleaner fit; the reader article-page render belongs to rows #28 (publish) and #29 (versioning). | — |
| 28 | Publish & unpublish ✅ DONE | lib/pkp | `publish-unpublish.spec.js` | `AmwandengaSubmission.cy.js` tests 6–7 | E: seed `submissionDraft({tag})` + a `skipExternalReview` decision so the submission lands at copyediting, plus a `publications[0]` entry with `versionStage: 'VoR'`, `published: false`, and `issue: {volume: 1, number: 2, year: 2014}` — i.e. an unpublished VoR pre-assigned to the bootstrap's published issue, which is the shape `AmwandengaSubmission.cy.js` reaches after its tests 1–5 (those decision steps are already covered by rows #19-26, so we shortcut past them). dbarnes opens Publication → Title & Abstract and drives the full round-trip via new `EditorialWorkflowPage` helpers: `publishCurrentPanel()` (click Schedule For Publication → Review Publishing Details side-modal: "Assign To Current/Back Issue" radio + `select[name=issueId]` = Vol. 1 No. 2 (2014) + `select[name=versionStage]` = VoR + `select[name=versionIsMinor]` + Confirm → `.pkpWorkflow__publishModal` "All publication requirements have been met" → Publish) and `unpublishCurrentPanel()` (click Unpublish → `[data-cy="dialog"]` Confirm). After each flip the spec asserts publication status via REST (`STATUS_PUBLISHED`=3 with `datePublished` set; `STATUS_QUEUED`=1 after unpublish) and opens a clean `browser.newContext` (no storageState) to verify the reader article URL — 200 with the title rendered when published, 404 when unpublished. The round-trip is publish → verify 200 → unpublish → verify 404 → republish → verify 200 so every transition direction is exercised once on the same seeded submission. | — |
| 29 | Versioning ✅ DONE (partial) | lib/pkp | `versioning.spec.js` | `AmwandengaSubmission.cy.js` tests 8–10 | E+R: seed `submissionPublished({tag})` so v1 is a published VoR. dbarnes drives the full versioning lifecycle via new `EditorialWorkflowPage` helpers: `createNewVersion()` (click Create New Version in the Publication side-nav → `[data-cy="dialog"]` `select[name=versionStage]`=VoR + `select[name=versionIsMinor]`=true + Confirm), then reloads the workflow page (the Vue side-nav caches the publications list locally so v1.1's sub-items only appear after a full reload) and asserts via REST that a second publication row exists with `versionMajor=1`, `versionMinor=1`, `STATUS_QUEUED`. Opens v2's Title & Abstract via `openPublicationPanel('Title & Abstract', {version: 'last'})`, edits the TinyMCE title to `"V2 <tag>"`, saves, waits for the `[role="status"]` "Saved" toast. Publishes v2 via `publishCurrentPanel()` — the same helper handles v2's label divergence (v2 exposes a plain "Publish" button instead of v1's "Schedule For Publication", both open the identical Review Publishing Details side-modal). R: opens a clean anonymous `browser.newContext` at `/index.php/publicknowledge/article/view/{id}` — asserts 200, `h1` shows the v2 title, and the `.versions` picker contains an `a[href*="/version/{v1PublicationId}"]` anchor labelled "Version of Record 1.0"; following that link asserts the URL contains `/version/{v1PublicationId}`, the v1 title renders, and the "outdated version" notice is visible. Unpublishes v2 via `unpublishCurrentPanel()` and reloads the anonymous page — asserts 200 again, `h1` shows v1's title, and the `.versions` picker no longer contains the v2 title. Second test exercises v2-aware contributor add: with v2 created on the same submission, dbarnes opens v2's Contributors panel via `openPublicationPanel('Contributors', {version: 'last'})`, drives Add Contributor through the same form flow as row #27 (givenName + familyName + email + Author role), Saves, captures the publication-scoped POST URL (the regex extracts the targeted publicationId), then individually fetches both publications via REST and asserts the new author lands ONLY on v2 — proving the version-scoped contributor pipeline binds to v2 and not v1. The publication LIST endpoint returns thin authors summaries, so per-publication GETs are needed for full author data. v2 galley + v2 issue panel edits remain deferred (galley CRUD lives in row #51; the Issue panel save is the same Vue widget that wasn't tractable in row #27). v2 title editing already lands in test 1, so the `pkpForm` save infrastructure on v2 is proven; deferred panels share the same infrastructure. Cypress's urlPath-based reader URL (`/article/view/mwandenga`) is dropped — the numeric `/article/view/{submissionId}` URL is the canonical reader route used by every other reader assertion in this migration. | — |
| 30 | Issue assignment ✅ DONE | ojs | `issue-assignment.spec.js` | `AmwandengaSubmission.cy.js` | E+R: E0 scratch journal with two published issues seeded via `JournalScenarioController.afterContextCreated` (Vol. 1 No. 1 (2026) + Vol. 7 No. 2 (2026)). Seed `submissionPublished({tag})` on the scratch journal targeting the source issue. dbarnes opens Title & Abstract → `unpublishCurrentPanel()` (status flips back to STATUS_QUEUED so the next publish click takes the full Review Publishing Details path again — sidesteps the deferral note's status-race) → `publishCurrentPanel({issueLabel: 'Vol. 7 No. 2 (2026)'})` to swap the issue. REST asserts the publication's status flipped QUEUED→PUBLISHED and issueId moved source→target. R: anonymous reader visits `/issue/view/{targetId}` — TOC link with the article title is visible; visits `/issue/view/{sourceId}` — same title is no longer in the TOC. Dropped (scope) the unassign arm (NO_ISSUE option = `Don't Assign To An Issue`, sets issueId=null + STATUS_READY_TO_PUBLISH) — its reader surface is the continuous-publishing list, not an issue TOC, so it belongs in its own row. Dropped (scope) driving the standalone Issue-panel save (the path the original deferral note pointed at) — the unpublish + re-publish flow exercises the same UI invariants (issue dropdown + status flip + issueId persistence) without modal-stacking on a publication that's already READY_TO_PUBLISH. | — |
| 31 | DOI assignment ✅ DONE | ojs | `doi-assignment.spec.js` | `Doi.cy.js` | E: three tests on E0 scratch journals — (a) auto-DOI on a newly-published article: `enableDois=true` + `doiPrefix=10.9999` + a seeded published issue (Vol 1 No 1 2026); seed `submissionPublished` on the scratch journal; assert the current publication's `doiObject.doi` starts with the seeded prefix (auto-mint via AssignDOIs listener on the scenario's acceptance decision) AND anonymous `/article/view/{id}` renders `<section class="item doi">` with the minted DOI. (b) versioned DOIs: with `doiVersioning=true`, seed v1 via `submissionPublished` + chain v2 with `versionIsMinor=false` so the `Repo::publication()->version()` call resets doiId and VersionDois listener mints a fresh DOI on v2's publish — assert v1 and v2 both carry prefix-scoped DOIs and the two values differ. (c) DOI configuration UI: scratch journal with DOIs OFF, manager opens Settings → Distribution → DOIs tab via `#dois-button`, ticks `enableDois`, ticks the publication/issue/representation `enabledDoiTypes` checkboxes, sets `doiPrefix` + `doiCreationTime=copyEditCreationTime`, saves (race with context PUT), reloads, asserts every value persisted (mirrors Cypress `checkDoiConfig`). Dropped manual-assign UI (Cypress `.skip`'d that flow on bug pkp-lib#10606) and the deposit-state mark-registered/mark-unregistered/mark-stale variants (Cypress `.skip`'d those for the same bug; deposit flow proper lives in row #32's Crossref spec). `ContextBuilderProcessor` extended with `enableDois`, `doiPrefix`, `doiVersioning`, `enabledDoiTypes`, `registrationAgency`, `onlineIssn`, `printIssn`, plus a `plugins` passthrough for enabling lazy-load plugins and seeding their settings. `JournalScenarioController` gained an `afterContextCreated` hook that routes an `issues` passthrough to the existing `IssueProcessor` inside the scratch-journal transaction. | **E0** (extended) |
| 32 | DOI Crossref registration ✅ DONE (partial) | ojs | `doi-crossref.spec.js` | `DoiCrossref.cy.js` | E: two tests on E0 scratch journals — (a) seed-via-passthrough setup with DOIs on + Crossref plugin enabled + `registrationAgency=crossrefplugin` + depositor credentials, seed `submissionPublished`, GET `/api/v1/contexts/{id}` and assert `registrationAgency` is `crossrefplugin`, PUT `/api/v1/dois/submissions/markRegistered` and re-fetch — `doiObject.status` is `Doi::STATUS_REGISTERED` (3) and the DOI string still carries the seeded prefix. (b) Settings UI flow: scratch journal without the plugin pre-enabled, manager opens Settings → Website → Plugins, ticks the legacy grid checkbox `input[id^="select-cell-crossrefplugin"]` (clicks via `force: true` to bypass jQuery-handler racing), then opens Distribution → DOIs → Registration sub-tab via `#doisRegistration-button`, picks `crossrefplugin` from the registration-agency select, fills depositor name + email, saves (race with context PUT), reloads, asserts everything persisted (mirrors Cypress `Check Crossref Configuration` + REST sanity-checks `registrationAgency`). **Dropped (deferred):** `PUT /api/v1/dois/submissions/export` round-trip — probing it on the scratch-journal seed runs into a PHP fatal in `lib/pkp/classes/xslt/XMLTypeDescription.php:141` ("Maximum execution time of 30+2 seconds exceeded"); the Crossref XSD validation pass is too slow to complete under the test PHP server's `max_execution_time` (single-threaded, no opcache for the schema). Reopen when the validation step moves off the request lifecycle. Actual Crossref HTTP deposit dropped (needs network/credentials). | **E0** (extended) |
| 33 | Publication language change ✅ DONE | ojs | `publication-language-change.spec.js` | `ChangeSubmissionLanguage.cy.js` | E+R: seed `submissionPublished({tag, metadata: {title: {en: 'Original title …'}}})` so v1 ships as a published English VoR. dbarnes opens Title & Abstract; first the spec asserts the change-language affordance is *suppressed* — the `WorkflowChangeSubmissionLanguage` widget renders inside `[data-cy="workflow-controls-left"]` only when `submission.status !== STATUS_PUBLISHED && publications.length < 2`, so its "Change" button has count 0 here. `unpublishCurrentPanel()` flips status to QUEUED; the "Change" button now appears. Click opens a `<div id="changeSubmissionLanguage">` SideModal with a PkpForm whose `locale` field is a `FieldRadioInput` (one `<input value="…">` per supported locale) + a `metadata` group (showWhen ≠ current) carrying TinyMCE `changeSubmissionLanguageMetadata-{title,abstract}-control` editors. Pick fr_CA, seed the French title/abstract, race a `page.waitForResponse` matching `/api/v1/submissions/{id}/publications/{pubId}/changeLocale` (Form.vue tunnels PUT through POST + `X-Http-Method-Override: PUT`, and the modal store calls `window.location.reload()` on success — without the response race the original port timed out behind the navigation). Assert the response is 200 and `submission.locale === 'fr_CA'` via REST. Republish via `publishCurrentPanel()` and assert the publication is `STATUS_PUBLISHED` with `locale: 'fr_CA'`. R: anonymous `browser.newContext` visits `/index.php/publicknowledge/fr_CA/article/view/{id}` (the locale-prefixed reader URL — the article template's `getLocalizedTitle()` resolves against the request locale, so the `/en/...` URL would still render the EN fallback that `changeLocale` keeps in `title.en`) and asserts the seeded French title appears in the `<h1>`. **Bootstrap caveat** — `publicknowledge` declares `supportedLocales: ['en', 'fr_CA']` but `JournalProcessor` doesn't pass that through to `supportedSubmissionMetadataLocales` / `supportedAddedSubmissionLocales` (and `PKPContextService::add` then defaults both to `['en']`), so the validator rejects fr_CA with `{title: {fr_CA: ["This language is not accepted."]}}`. Spec ships a one-shot idempotent `ensureSubmissionMetadataLocale()` helper that PUTs `/api/v1/contexts/{id}` to add fr_CA to those four locale arrays — same one-time touch the original Cypress suite did via the Languages settings grid in `20-CreateContext.cy.js`. Promoting the helper into `bootstrap.js` would need a `lib/pkp` change (out of scope per submodule discipline). Dropped (scope) the "change back to English + republish" arm — the round-trip's terminal capability is "fr_CA persists across publish", which the assert chain above already proves end-to-end. | — |
| 34 | Article DC metadata ✅ DONE | ojs | `article-dc-metadata.spec.js` | `Z_ArticleViewDCMetadata.cy.js` | R: core DC meta tags (Title, Creator.PersonalName, Identifier, Identifier.URI, Type, Type.articleType, Language, Date.issued, Source, Source.ISSN) on an anonymous article view; DC.Subject one-per-keyword and DC.Rights one-per-copyrightHolder + one-per-licenseUrl. Dropped (scope) DC.Coverage / DC.Type (localized) / DC.Description — require the Metadata-settings toggles (row #16) on an E0 scratch journal; DC.Title.Alternative — needs a multilingual publication fixture (row #5 territory); DC.Format — blocked on E1 galley seeding (row #51); DC.Identifier.DOI — blocked on row #31 DOI enablement. The DublinCoreMeta plugin is enabled by default on every OJS journal (`settings.xml` ships `enabled=true`), so the bootstrap `publicknowledge` journal emits these tags without additional config. | — |
| 35 | Journal homepage ✅ DONE | ojs | `journal-homepage.spec.js` | derived from implicit reader coverage | R: journal index (`/publicknowledge/`) — 200, `<title>` contains "Journal of Public Knowledge", h1 visible, default-theme `Current Issue` h2 present, bootstrap issue "Vol. 1 No. 2 (2014)" identifier rendered (seeded a published submission so the current-issue section has TOC content); issue archive (`/issue/archive`) — 200, `.page_issue_archive` wrapper, h1 matches "Archives", `ul.issues_archive` contains the bootstrap issue. Dropped (scope) "section grouping on TOC" — requires seeded published articles with galleys (rows #30 + #51, blocked on E1/E5). | — |
| 36 | Article statistics ✅ DONE | ojs | `article-statistics.spec.js` | `Statistics.cy.js` | E: seed `submissionPublished`, dbarnes opens `/stats/publications`, assert h1 "Articles", the `.pkpStats__graph` chart surface, the `Abstracts`/`Files` timeline-type toggles, the date-range picker, and the `Article Details` detail-table (`h2#publicationDetailTableLabel` + `Abstract Views`/`File Views` column headers). Dropped the Cypress metrics-seeded assertions (generateTestMetrics.php shell-out, per-author row search, date-range picker apply/filter set) — the capability here is "page mounts + structural landmarks", and porting the metrics generator is separate infrastructure. | — |
| 37 | Pubmed metadata ✅ DONE | ojs | `pubmed-metadata.spec.js` | `Pubmed.cy.js` | E: seed `submissionPublished`, dbarnes hits `/management/importexport/plugin/PubMedExportPlugin/exportSubmissions` (CSRF-guarded form POST), asserts the returned PubMed/MEDLINE XML wraps `<Article>` with `<Issn>0378-5955</Issn>` (bootstrap's onlineIssn) + `<ArticleTitle>` carrying the seeded tag; anonymous access to the same endpoint is rejected (non-2xx, no XML body). Dropped the doc cell's "Pubmed meta tags on article page" and "plugin config" framing — PubMed is an `importexport` plugin with no article-page output, and it's auto-loaded on every OJS journal (no enable step), so the real capability is the XML export. No E0 needed. | — |
| 38 | Public comments — reader side ✅ DONE | lib/pkp | `public-comments.spec.js` | `lib/pkp/.../publicComents/PublicComments.cy.js` | E: E0 scratch journal with `enablePublicComments=true` (new ContextBuilderProcessor passthrough) + a published submission; phudson (baseline reviewer) posts via `POST /{journal}/api/v1/comments` with the CSRF token pulled from `window.pkp.currentUser.csrfToken` on `/index/user/profile`; the anonymous article page confirms the unapproved comment stays hidden; dbarnes (scratch-journal manager) then approves via `PUT /{journal}/api/v1/comments/{id}/setApproval`; the anonymous reader reloads and sees the approved body rendered inside `<section id="public-comments">`. · R: login gate — anonymous visit renders the comments section but `<textarea>` count is 0 (the `PkpCommentsNewInput` textarea is gated to authenticated users). Public comments is a core context feature (not a plugin) — the roadmap cell's "enable comments plugin" framing is off; the real gate is the `enablePublicComments` context schema flag, now surfaced through the ContextBuilder. Moderator UI surface (userComments management page) split into row #61. | **E0** |
| 61 | Public comments — moderator UI ✅ DONE (partial) | lib/pkp | `user-comments-moderation.spec.js` | `lib/pkp/.../publicComents/PublicComments.cy.js` (the 14 of its 24 `it()` blocks that exercise the moderator side) | E: structural-only at this stage. Two tests on E0 scratch journals — (1) page mount: heading "Comments" renders + four tabs (All / Approved / Hidden/Needs Approval / Reported) render with the localized labels + the empty state shows "No Items"; (2) tab navigation: clicking each tab flips `aria-selected="true"` to that tab. **Deferred:** Approve / Hide / Delete via the detail modal, Reports tab + report-deletion flow, version-closes-discussion gating, delete-own/delete-others authorization. Probing those surfaces hit a flaky `useFetchPaginated` reactivity quirk specific to scratch-journal contexts — the API reliably returns the seeded comment but the table sometimes never renders the row (page stays in "No Items" empty state even after the fetch resolves). The same page on bootstrap publicknowledge works fine, suggesting a per-session context-cache or watch-immediate timing race in the SPA store. Reopen when either (a) a scenario passthrough seeds comments inside the journal-create transaction so they're visible BEFORE the SPA mounts, or (b) the userCommentStore reactivity is hardened against cold-cache scratch-journal sessions. The reader-side approve flow + REST setApproval round-trip already lives in row #38 — this row covers the OTHER half of the surface the audit flagged. | **E0** |
| 39 | OAI — DC endpoint ✅ DONE | lib/pkp | `oai-dc.spec.js` | `lib/pkp/.../oai/DC.cy.js` | R: seed `submissionPublished({tag})` and hit `/publicknowledge/oai` as anonymous `APIRequestContext`. (1) `verb=ListRecords&metadataPrefix=oai_dc` returns 200 + `text/xml`, contains `<ListRecords>`, at least one `<record>` with `<oai_dc:dc>` wrapping `<dc:title>` + `<dc:creator>` + `<dc:source>Journal of Public Knowledge</dc:source>` + `<dc:language>en</dc:language>` + `<dc:identifier>…/article/view/{id}</dc:identifier>`, and one of those titles contains the seeded tag. (2) Extract the tagged record's `<identifier>`, assert shape `oai:{repositoryId}:article/{id}`, then `verb=GetRecord&metadataPrefix=oai_dc&identifier=…` returns 200 + `<GetRecord>` echoing the identifier and the seeded title, with no `<error>` element. Regex-based XML parsing (no XML parser import). | — |
| 40 | Data-availability statements ✅ DONE (partial) | lib/pkp | `data-availability.spec.js` | `lib/pkp/.../DataAvailabilityStatements.cy.js` | E: on an E0 scratch journal, dbarnes toggles Workflow → Metadata's `dataAvailability` FieldMetadataSetting from disabled to Request, saves via the shared context PUT, reloads the settings page and asserts the enable checkbox + `value="request"` radio are both persisted · R: seed `submissionPublished({tag})` with `publications[0].metadata.dataAvailability = {en: ...}` (PublicationsProcessor already lists `dataAvailability` in METADATA_FIELDS, so no scenario extension), anonymous `browser.newContext` navigates to the article page and asserts `<section id="data-availability-statement">` renders the "Data Availability Statement" heading + the seeded statement body. Dropped (scope) the workflow-side TinyMCE round-trip (same `edit() → Saved` path already covered by row #27's title/abstract assertion) and the reviewer visibility matrix (Cypress source gates both on a never-set env flag; belongs with row #49 once E2 lands). | **E0** |

### Wave 5 · Access control

| # | Feature | Home | Spec file | Cypress source | Tests | Ext? |
|---|---|---|---|---|---|---|
| 41 | Recommend-only editor restrictions ✅ DONE | lib/pkp | `recommend-only-editor.spec.js` | `AmwandengaSubmission.cy.js` tests 11–12 | E: seed `submissionInReview` with dbarnes (full editor) + minoue (sectionEditor, `recommendOnly: true`). Log in as minoue, assert the WorkflowRecommendOnlyControls panel shows Recommend Accept / Recommend Revisions / Recommend Decline and NOT the full decision buttons (Accept Submission / Decline Submission / Request Revisions). Log in as dbarnes and assert the inverse — full decision buttons visible, recommendation buttons absent. "Recommend Resubmit for Review" dropped — it surfaces only after completed reviews (WorkflowStageDao), and the seeded round has none. The row's publish/unpublish/create-version restrictions (the Cypress test's continuation) are covered by row #28 / #29 from the full-editor side; the recommend-only gate itself is the two-surface render this spec asserts. | — |
| 42 | Section-editor metadata permissions ✅ DONE | lib/pkp | `section-editor-metadata.spec.js` | `AmwandengaSubmission.cy.js` test 12 | E: seed `submissionPublished` with minoue as a sectionEditor participant in two variants (`canChangeMetadata: false` vs `true`). Log in as minoue, open Publication → Title & Abstract, assert Save is disabled in the false variant and enabled in the true variant. `ParticipantProcessor` already honoured the flag from row #22; `submission-published.js` gained a `participants` override (OJS-root fixture, mirror of submission-in-review.js's row #22 extension) so the two variants seed declaratively. Login-As / participant-edit-form round-trip dropped — the feature is the gate's two states, not the toggle form (the form is generic fbv reachable from every participant row). | — |
| 43 | Author edit-publication permission ✅ DONE | lib/pkp | `author-edit-published.spec.js` (`author with canChangeMetadata=false sees a disabled Save on Title & Abstract`, `author with canChangeMetadata=true can Save Title & Abstract`) | `AmwandengaSubmission.cy.js` tests 4–5 | E: P0 baseline added a non-privileged `atester` author user (`lib/pkp/playwright/data/users.js`, journal=publicknowledge, roles=['author'], mustChangePassword=false → password derives to 'atesteratester'). Seed `submissionDraft({submitter: 'atester'})` and log in as atester via `/dashboard/mySubmissions?workflowSubmissionId=…` (authors don't have access to `/dashboard/editorial`); SubmissionBuilder's auto-author StageAssignment inherits the Author UserGroup's `permitMetadataEdit=false`, so Save is disabled on Title & Abstract — the canEditPublication gate's default-deny branch. For the canChangeMetadata=true variant, switch the submitter to rvaca and add atester explicitly via `participants: [{user:'atester', role:'author', canChangeMetadata:true}]`; the auto-author StageAssignment for the submitter is reused via `Repo::stageAssignment()->build()`'s `firstOr`, so atester gets a fresh assignment with the spec flag preserved end-to-end. Tests verify Save toggles enabled/disabled accordingly. Cypress test 3 (editor-side full-pane edit walkthrough) and the author-side galleys/contributors/Login-As round-trip dropped — separately covered by rows #28 / #29 / #41 / #42 / #44; this row's invariant is the canEditPublication gate's two states. | — |

### Wave 6 · Admin cross-cutting

| # | Feature | Home | Spec file | Cypress source | Tests | Ext? |
|---|---|---|---|---|---|---|
| 44 | Login-as (impersonation) ✅ DONE | lib/pkp | `login-as.spec.js` | `AmwandengaSubmission.cy.js` test 13 | E: site admin impersonates dbarnes via the canonical `/login/signInAsUser/{userId}` route, `window.pkp.currentUser` flips to `{username: 'dbarnes', isUserLoggedInAs: true, loggedInAsUser: {username: 'admin'}}`; `/login/signOutAsUser` rolls the session back to admin with `isUserLoggedInAs` cleared. Driven through the URL both the Users & Roles admin grid's `logInAs` link action and the stage-participant grid's `Login As` button navigate to after their RedirectConfirmationModal OK, keeping the spec anchored on the server-side state transition rather than the legacy jQuery grid UX. Dropped (scope) the Cypress source's `workflowSubmissionId` redirect round-trip — that's a legacy stage-participant grid concern; the site-level capability is what the roadmap asks for. | — |
| 45 | Jobs queue ✅ DONE (partial) | lib/pkp | `jobs-queue.spec.js` | `lib/pkp/.../Jobs.cy.js` | E: site admin reaches `/index/admin/jobs` and `/index/admin/failedJobs` — both pages mount, render h1 + PkpTable landmarks, and the backing `/api/v1/jobs/all` and `/api/v1/jobs/failed/all` endpoints return `{data, total, ...}` JSON. `ReauthenticationRequiredPolicy` is a no-op under the test config (`security.password_timeout=0`). Dropped (scope) the Cypress suite's enqueue → process → fail → delete → redispatch round-trip — it shells out to `php lib/pkp/tools/jobs.php {test,run,purge,failed}` via `cy.exec`, which has no Playwright equivalent. The admin-page structural render + API resolution is the "jobs admin surface reaches Laravel" invariant; the enqueue/process lifecycle is covered by lib/pkp/tests/jobs/ unit coverage. | — |
| 46 | Multiple contexts ✅ DONE | ojs | `multiple-contexts.spec.js` | `MultipleContexts.cy.js` | E: E0 scratch journal with dbarnes as manager (dbarnes is already a publicknowledge editor via the bootstrap, so she now holds two distinct role sets in two different contexts). Anonymous site index (`/index`) lists both journals via the default OJS skin's `.journals a[rel="bookmark"]` anchors. As dbarnes, `/publicknowledge/dashboard/editorial` renders with `.app__contextTitle = "Journal of Public Knowledge"` and the `.app__contexts` header dropdown lists an anchor pointing at the scratch journal's URL path; navigating to the scratch journal's editorial dashboard succeeds, flips `.app__contextTitle` off publicknowledge, and the dropdown's contents invert (publicknowledge appears there instead); hopping back to publicknowledge works without a reauth prompt. Dropped (scope) the Cypress source's journal enable/disable UI round-trip — that exercises the legacy `contextGrid` jQuery UI, not multi-context role scoping. | uses **E0** as tool |
| 47 | API smoke ✅ DONE (partial) | lib/pkp | `api-smoke.spec.js` | `lib/pkp/.../API.cy.js` + `cypress/.../API.cy.js` | E: four tests — (1) authenticated `/user/profile` exposes `window.pkp.currentUser.csrfToken` matching `/^[A-Za-z0-9]{16,}$/`, the speculative `/index/api/v1/_csrf` endpoint returns 404/401/403 (flag for cleanup in `lib/pkp/playwright/support/api.js`); (2) anonymous `/api/v1/submissions` returns 401 with a JSON `{error: …}` body; (3) dbarnes's session-authenticated `/api/v1/submissions` returns 200 with `{items[], itemsMax: number}` shape; (4) atester (baseline non-editor author) lists submissions and the response includes a draft submission seeded with atester as the submitter — mirrors the OJS-side Cypress source's "author sees their own item" assertion, anchored on submission id (not item count) for parallel safety. Session auth replaces the Cypress apiToken round-trip — the apiToken path requires a runtime `api_key_secret` mutation on `config.test.inc.php` which is fragile under parallel workers, and session auth exercises the same HasUser middleware gate. Dropped (scope) the ApiProfileForm create/delete-key UI (legacy jQuery profile form, distinct concern from "API is reachable"). | — |
| 59 | Site admin Add Journal UI ✅ DONE | ojs | `admin-add-journal.spec.js` | `cypress/tests/data/10-ApplicationSetup/20-CreateContext.cy.js` | E: site admin opens `/index/admin/contexts`, drives the contextGrid's `createContext` link action which mounts the `<add-context-form>` (PKPContextForm in editContext.tpl) inside a side modal labelled "Create Journal". Fills `name-en` (multilingual, `localizedName` per FieldBase #110-112), `acronym-en`, `contactName`, `contactEmail`, picks `country=Canada` from the FieldSelect, and (for installs with multiple locales — the test installer adds fr_CA via `additionalLocales`) ticks `supportedLocales[value=en]` + `primaryLocale[value=en]`. Validation arm A: submits an invalid `urlPath` containing a space — server returns the `urlPath.regex` error (`admin.contexts.form.pathAlphaNumeric`, "The path can only include letters…"). FieldError renders inline as `<div class="pkpFieldError">` adjacent to the urlPath input. Validation arm B: switches the path to `publicknowledge` (the bootstrapped journal's path) — the `pathExists` after-validator (`PKPContextService.php#315`) returns "already in use". Happy path: switches to a unique tagged urlPath, server creates the journal, AddContextForm.success() redirects via `window.location` to `/admin/wizard/{id}` (the contextSettings.tpl Settings Wizard with `<h1>Settings Wizard</h1>`). REST sanity-check via `/index/api/v1/contexts?searchPhrase={acronym}` (admin-scoped — contextController routes `add` under `Role::ROLE_ID_SITE_ADMIN`) returns the new context with matching urlPath. Anonymous reader hits `/index.php/{path}/`, gets 200, and sees the seeded journal name. **Surprises**: (1) The Create UI is a Vue **side modal** ("Create Journal" header), not a jQuery UI AjaxModal — the Cypress source's `div[id=editContext]` selector still works because the inner Vue mount-point retains that id, but the surrounding chrome is reka-ui side-panel. (2) `country` is non-nullable per `lib/pkp/schemas/context.json` and is validated server-side with the `country` Laravel rule — leaving the Country select on its empty default surfaces "This is not a valid country" on the FIRST submit and POISONS subsequent submits because `FormPage.vue#65` gates Save with `(isLastPage && !!Object.keys(errors).length)`, so any lingering error keeps Save disabled even after the urlPath field is corrected. The spec must select a country before the first submit. (3) `searchPhrase` on `/api/v1/contexts` only matches against `description`/`acronym`/`abbreviation` settings (`PKPContextQueryBuilder.php#200`), not against `urlPath` or `name`, so the REST sanity-check searches by the seeded acronym. (4) Settings Wizard auto-launches via `AddContextForm.success() → window.location.href = editContextUrl.replace('__id__', r.id)` — no manual navigation needed. **Dropped** the Cypress source's TinyMCE description fields, the multilingual French-tab toggle, and the empty-fields-required cascade — the row's invariant is the urlPath validator's two error paths + the post-create Settings Wizard landing, and validating those without the noise keeps the spec under 200 LOC. The Settings Wizard tab walkthrough (Appearance/Languages/Indexing/Context tabs) is its own row's territory. | — |
| 60 | Manager assigns user to role (non-invite) ✅ DONE (partial) | lib/pkp | `user-role-assignment.spec.js` | `lib/pkp/cypress/support/commands.js` (`inviteUser` helper, non-email branch) | E: E0 scratch journal seeds dbarnes=manager and phudson=reviewer (so phudson surfaces in the Users grid). dbarnes opens Users & Roles, drives phudson's row "More Actions" → Edit, lands on `/management/settings/user/{id}` — the editUser branch of the userRoleAssignment wizard, which `SendInvitationStep::getSteps` short-circuits to two steps (Enter Details + Email Composer) when `$user` is set. dbarnes clicks "Add Another Role", picks "Author" (selectOption by visible label — `availableUserGroups` excludes Reviewer because phudson already holds it), fills today's `dateStart` + masthead "Appear on the masthead", advances to the email step (the auto-loaded UserRoleAssignmentInvitationNotify body needs no edits), submits "Invite user to the role". Asserts the "Invitation Sent" reka-ui PkpDialog mentions phudson@mailinator.com, then re-fetches `/api/v1/invitations/userRoleAssignment` via the same authenticated context — one PENDING row exists for phudson with userGroupsToAdd containing the Author user-group name. Returns to the access page and verifies the Invitations panel header flips from "Invitations (0)" to "Invitations (1)". **Surprise — the OJS Users & Roles UI exposes no non-invite path**: every role-addition (Invite-to-a-role + Edit-user) routes through `app(Invitation::class)->createNew('userRoleAssignment')`, ends with an email composer, and writes a PENDING invitation rather than mutating `user_user_groups` directly. Even the editUser flow's wizard sends an email with `Accept Invitation` / `Decline Invitation` links; the role only attaches to the user once they click through (row #57 territory). Spec stops at the pending-invitation REST sanity-check — the manager-side UI plumbing is what's distinct from row #57's new-email branch (which ALSO drives the email click-through + registration form). The "user not yet in any scratch-journal role" plan requirement was dropped — the wizard's user search filters by `contextId` server-side, so a not-in-context user is invisible to the search step and falls into the same new-user branch row #57 covers. The "log in as the assignee" arm dropped for the same reason. | uses **E0** |
| 57 | User invitation flow (multi-actor) ✅ DONE | lib/pkp | `user-invitation.spec.js` | `lib/pkp/cypress/tests/data/10-ApplicationSetup/40-CreateUsers.cy.js` (`createUserByInvitation` helper) | E: E0 scratch journal with dbarnes as manager. Manager-side three-step wizard: Search User (input name=`search` is filled with a unique-tag `invitee-{tag}@mailinator.com`; `UserInvitationSearchFormStep`'s registered action runs `/api/v1/users?searchPhrase=…`, finds nothing, stashes the value as `inviteeEmail` and lets the wizard advance), Enter Details (multilingual `givenName-en`/`familyName-en` + the pre-rendered `userGroupsToAdd[0]` row — picks Reviewer by visible label, today's `dateStart`; Reviewer is one of `reviewerUserGroupIds` so masthead is a static span with no select to drive), Email Composer (auto-loaded `UserRoleAssignmentInvitationNotify` body, "Invite user to the role" submits to `/api/v1/invitations/{id}/invite`). Asserts "Invitation Sent" reka-ui PkpDialog mentions the invitee email. Email-side: `pkpMail.latestTo(inviteeEmail)` + `fullMessage(id)` round-trips the dispatched mail, a local `extractAcceptUrl` regex pulls the `<a class='btn btn-accept'>` href out of the HTML body. Invitee-side anonymous `browser.newContext` navigates to the accept URL, waits for `store.email` to render (anchored on the email-display block, i.e. the `inviteeEmail` text), then drives the AcceptInvitation Vue page's three-step wizard: Step 1 userCreate (username + password + privacy-consent checkbox; `expect(privacyCheckbox).toBeChecked()` forces the FieldOptions emit to land before Save), Step 2 userDetails (multilingual `affiliation-en` + non-multilingual `userCountry` select — manager's invitation never collected those), Step 3 review + finalize (button label "Accept And Continue to OJS", post-finalize success dialog with `acceptInvitation.modal.title` text). REST round-trip via the manager's authenticated context: `/api/v1/users?searchPhrase={email}` returns the new row with matching `userName` + a `groups` entry whose name matches `/reviewer/i`. Final fidelity check logs in as the new user via the standard login form and waits for `/dashboard/(editorial|mySubmissions|reviewAssignments)`. **Surprises**: (1) The accept-button locale key `acceptInvitation.detailsReview.nextButtonLabel` has its en translation in the **OJS-specific** `locale/en/invitation.po` ("Accept And Continue to OJS"), NOT the shared `lib/pkp/locale/en/invitation.po`; OMP/OPS override the same key with their own brand strings, so the spec anchors on the `^Accept And Continue` prefix to stay application-agnostic. (2) The PKP `finalize` endpoint does NOT auto-log-in the new user — clicking "View All Submissions" on the success dialog redirects through `/login`. The spec drives an explicit second login as the post-finalize fidelity step. (3) The OJS login form's password input ships `maxlength="32"` (loginForm.tpl); the AcceptInvitation wizard's password field has no such cap, so a long generated password (e.g. `username + username` per `data/users.js#getPassword`, which produces 40+ chars for tagged usernames) writes a hash on accept that no later login can match — Playwright's `.fill()` honours maxlength and silently truncates. The spec uses a short literal password (`Inv1tee!Pwd`, 11 chars) instead. (4) `pkpMail.extractLink` matches double-quoted hrefs only, but `emails.userRoleAssignmentInvitationNotify.body` (lib/pkp/locale/en/emails.po:606-622) ships single-quoted hrefs AND a `class='btn btn-accept'` attribute between href and `>`; spec uses a local `extractAcceptUrl` helper anchored on the `btn-accept` class signature rather than retrofit the shared helper for one caller. **Drop list**: French-tab multilingual editing on the userDetails step (Cypress's `confirmationByUser` drove that — covered separately by row #34 / multilingual.spec.js); the success-dialog → "View All Submissions" landing assertion (post-finalize redirect bounces through login, so the explicit-login fidelity step subsumes it). | uses **E0** |

### Wave 7 · Blocked — need scenario-endpoint extensions (see §2)

| # | Feature | Home | Spec file | Cypress source | Tests | Ext? |
|---|---|---|---|---|---|---|
| 48 | Decision: request revisions ✅ DONE | lib/pkp | `decision-request-revisions.spec.js` (`editor requests revisions; author sees the upload-revisions affordance`) | `LkumiegaSubmission.cy.js` test 2 | E: seed `submissionInReview({submitter: 'atester'})`, dbarnes drives the new `EditorialWorkflowPage#clickRequestRevisions` helper (handles the `WorkflowSelectRevisionFormModal` side-modal radio + Next click that defaults to `PENDING_REVISIONS` — no new round) → notifyAuthors email step → Record Decision, asserts the completion message reads "have been requested" and a `DECISION_PENDING_REVISIONS` row lands in `/api/v1/submissions/:id/decisions`. Then atester logs in via `asUser('atester')`, hits `/dashboard/mySubmissions?workflowSubmissionId=:id`, the "Upload revisions" button (only visible once the review round flips to `REVIEW_ROUND_STATUS_REVISIONS_REQUESTED` — see workflowConfigAuthorOJS.js#229-242) is clicked to open the legacy `FileUploadWizardHandler` plupload modal ("Upload Review File"), atester picks Article Text genre + uploads `default-article.pdf` via setInputFiles on the opacity-0 plupload input, walks Continue / Continue / Complete (id=continueButton on every step per row #51 finding), and a REST GET `/api/v1/submissions/:id/files?fileStages[]=15` asserts the new file lands as `SUBMISSION_FILE_REVIEW_REVISION` (15). The original cell's E1/E2 markers are superseded by the scenario-extensions infrastructure (default Article Text file always seeded; no email tokens needed because `atester` logs in directly). | — |
| 49 | Reviewer completes review ✅ DONE | lib/pkp | `reviewer-completes-review.spec.js` | scattered in submission specs (`cy.performReview` from lib/pkp/cypress/support/commands.js#625) | E: seed `submissionInReview({reviewers:[{user:'phudson', status:'invited'}]})`. As phudson, navigate to `/reviewer/submission/{id}` and drive the four-step fbv wizard end-to-end: Step 1 ticks `input[name="privacyConsent"]` and clicks "Accept Review, Continue to Step #2" (flips REVIEW_ASSIGNMENT_STATUS_RESPONSE_OVERDUE→ACCEPTED); Step 2 clicks "Continue to Step #3" past the guidelines; Step 3 fills `comments` + `commentsPrivate` TinyMCE editors via the FBV-generated `id^="comments-"` / `id^="commentsPrivate-"` ids, picks "Accept Submission" from `select#reviewerRecommendationId`, submits and OK-confirms the PkpDialog; lands on the `reviewCompleted.tpl` "Review Submitted" page. REST round-trip via dbarnes asserts the assignment lands in `REVIEW_ASSIGNMENT_STATUS_RECEIVED` (or `STATUS_COMPLETE`) with `reviewerRecommendationId` resolving to the "accept" default recommendation row (lookup via `/api/v1/reviewers/recommendations/{id}` checks `defaultTranslationKey === 'reviewer.article.decision.accept'`). Original cell flagged E1/E2; both gates lifted — phudson logs in directly (no token), and the happy path doesn't need a file attachment. | — |
| 50 | Review-round lifecycle ✅ DONE | lib/pkp | `review-round.spec.js` (`editor closes round 1 with revisions and opens round 2 from the workflow UI`) | embedded (LkumiegaSubmission + AmwandengaSubmission) | E: seed `submissionInReview` with one *completed* reviewer (`phudson`, recommendation pendingRevisions — RequestRevisions only adds a notifyReviewers step when REVIEW_ASSIGNMENT_COMPLETED rows exist, and the completion is also the gate that surfaces the next decision). dbarnes drives `clickRequestRevisions` → notifyAuthors → notifyReviewers → Record Decision; then `clickDecision('Create New Review Round')` → notifyAuthors → PromoteFiles → Record Decision. Asserts both decision rows (`DECISION_PENDING_REVISIONS=4` + `DECISION_NEW_EXTERNAL_ROUND=14`) land in `/api/v1/submissions/:id/decisions`, and the `/api/v1/submissions/:id/reviewRounds` endpoint shows two stage-3 rounds with the round-2 id distinct from round 1's. Test 2 from the row's plan ("seeded round-2 state surfaces correctly") is **deferred** — its assertions overlap fully with `playwright/tests/scenarios/submission-in-round-2.spec.js`, which already round-trips the chained-decisions fixture through the submission API + workflow page; a second copy here would duplicate coverage without adding a guarantee. The original cell's E3 marker is superseded by the `submissionInRound2` fixture + ReviewRoundProcessor wiring that already shipped. | — |
| 51 | Galleys ✅ DONE | ojs | `galleys.spec.js` | `AmwandengaSubmission.cy.js` | E+R: seed `submissionPublished({tag})`, dbarnes opens Publication → Galleys, drives the new `EditorialWorkflowPage#addGalley` helper (which fills the legacy "Create New Galley" fbv form, advances the stacked 3-step file-upload wizard via `setInputFiles()` + the wizard's reused `button#continueButton`), and asserts the galley row appears with label "PDF". An anonymous `browser.newContext` then asserts `a.obj_galley_link` matching "PDF" with an `href` ending in `/article/view/{id}/pdf-{tag}` (URL path round-trip). A second test seeds the same submission, adds a PDF galley, then drives `#deleteGalley` (More Actions menu → Delete menuitem → fbv-style confirm dialog OK) and verifies the PDF link disappears from the reader page. Reuses the bundled `lib/pkp/playwright/fixtures/files/default-article.pdf` for the galley source. **Dropped**: the second HTML galley variant (a single PDF run already exercises the genre-agnostic upload pipeline; re-add when a galley-format-specific surface like inline-HTML rendering needs coverage), the Cypress edit / urlPath-error / "wait for jQuery" idioms (POM `expect(... visible)` waits replace the legacy waits), and the `cy.checkViewableGalley` iframe-src assertion (the obj_galley_link href is what the row's "download links" capability requires; the iframe is presentation). The roadmap cell's E1/E5 markers turn out not to be needed for this row — galley creation drives the UI's existing upload wizard (no scenario-side galley seeding required). | — |
| 52 | Subscription-based access ✅ DONE | ojs | `subscription-access.spec.js` | `Subscriptions.cy.js` (second half) | R: E0 scratch journal with `publishingMode=SUBSCRIPTION` + a published issue marked `accessStatus=ISSUE_ACCESS_SUBSCRIPTION` (new `ContextBuilderProcessor` + `IssueProcessor` passthroughs). Seed `submissionPublished({journal: scratchPath, …})` — fixture extended with a `journal` override (default still 'publicknowledge'). As dbarnes manager, drive two UI surfaces on the scratch journal: `/payments` Subscription Types grid creates one type (mirror of row #9), then Workflow → Publication → Galleys reuses row #51's `addGalley()` POM to attach a PDF galley. Three reader arms now ship: (1) **anonymous** — the seeded PDF `a.obj_galley_link.pdf` carries the `restricted` CSS class (`galley_link.tpl#56-62` → `hasAccess=false`); (2) **editor (dbarnes manager)** — same PDF link, not `restricted` — manager bypasses via `Repo::submission()->canPreview()` → `subscribedUser()` shortcut; (3) **subscriber (phudson)** — drives the legacy `IndividualSubscriptionsGridHandler` "Create New Subscription" AjaxModal end-to-end (subscriberSelect grid → userSearchForm radio pick → typeId/status selects → date inputs), saves, then re-opens the article URL with phudson's storageState and asserts PDF galley is not `.restricted` plus the abstract paragraph renders. phudson is enrolled into the scratch journal as 'reader' (the only non-editorial role) so he surfaces in `SubscriberSelectGridHandler`'s `filterByContextIds` collector without picking up `canPreview()` — only the `individual_subscriptions` row this test creates can grant access. **Locator notes for the subscription form**: link-action button id pattern lowercases the trailing `Handler` suffix (PKPHandler#setId) so the addSubscription action lives at `component-grid-subscriptions-individualsubscriptionsgrid-addSubscription-button-…`; jQuery UI datepicker renames the visible `<input name=dateStart>` to `name=dateStart-removed` on init (the canonical hidden alt-field becomes the form's `dateStart`), so the spec sets both inputs via JS + dispatches `change` rather than `fill()` into the visible one. Anchors on the *PDF* galley specifically — the auto-rendered JATS XML link in `article_details.tpl#341-348` is hardcoded outside `galley_link.tpl` and never carries the `restricted` class regardless of subscription state. | — |
| 53 | Native XML: submission ✅ DONE | ojs | `native-xml-submission.spec.js` | `lib/pkp/.../NativeXmlImportExportSubmission.cy.js` (seeds an OJS submission) | E: seed `submissionPublished({tag})`, dbarnes drives the Native plugin's two-step JSON export flow as raw API requests — POST `exportSubmissions` returns a JSONMessage carrying `exportedFileDatePart` / `exportedFileContentNamePart` hidden inputs; a follow-up POST to `downloadExportFile` streams the actual XML bytes. The XML round-trips the seeded `Published article [tag]` title and the canonical `<article>` wrapper. Reimport: POST the same XML as multipart `uploadedFile` to `uploadImportXML` → temporaryFileId; POST `import` (with `csrfToken` in form, since the legacy `$request->checkCSRF()` reads the form value not just the X-Csrf-Token header) runs the native-xml=>article filter chain. REST round-trip via `/api/v1/submissions?searchPhrase=...` confirms a NEW submission with the same tag-suffixed title joined the original on the list. Original E1 gating lifted — every seeded submission ships with the default Article Text file attached, so the export carries real `<article_galley><file>` content without spec-side seeding. | — |
| 54 | Native XML: issue ✅ DONE | ojs | `native-xml-issue.spec.js` | `Y_NativeXmlImportExportIssue.cy.js` | E: E0 scratch journal seeded with one published Vol. 1 No. 1 (2026) issue + one `submissionPublished` assigned to it; dbarnes drives the Native plugin's two-step JSON export against the scratch journal. Asserts the XML round-trips `<volume>1</volume>` / `<number>1</number>` / `<year>2026</year>` plus the assigned submission's tag-suffixed title via the embedded `<title>` chain. R: a second test exports an issue from one scratch journal and reimports the XML into a SECOND, fresh scratch journal — the two-journal pattern side-steps the single-journal collision the Cypress source dodged with a `<year>=currentYear+1` patch (the import filter binds issues to `$deployment->getContext()`, so a fresh journal has no conflicting issue rows). Verifies a new issue with matching volume/number/year surfaces in `/api/v1/issues` on the target and the seeded submission rides along into the target's `/api/v1/submissions?searchPhrase=tag` listing. **Locale-segment caveat**: POSTs to `/{journal}/{locale}/management/importexport/...` get a 302 to the locale-less URL that downgrades to GET and silently drops the form body (empty `selectedIssues` → "Missing child element issue" XSD error on export, `common.uploadFailed` on multipart import); spec uses the locale-less URL for both `exportIssues`/`downloadExportFile` AND `uploadImportXML`/`import`. The submission spec gets away with the locale prefix on its bootstrap-journal upload because publicknowledge's dispatcher is warmer; scratch journals always need the locale-less form. Original E1 gating lifted by the default-file scenario — the issue's submissions carry real `<article_galley><file>` content. | — |
| 55 | ORCID integration ✅ DONE | lib/pkp | `orcid.spec.js` | `lib/pkp/.../orcid/Orcid.cy.js` + `cypress/.../orcid/Orcid.cy.js` | E0 scratch journal + dbarnes manager. Five arms: (1) **config persists** — manager opens the Access Settings page's `#orcidSettings` tab, ticks `orcidEnabled`, picks `memberSandbox` API type, sets `orcidClientId=TEST_CLIENT_ID` / `orcidClientSecret=TEST_SECRET` / `orcidCity=Vancouver` / `orcidSendMailToAuthorsOnPublication` / `orcidLogLevel=INFO`, races Save against the `/api/v1/contexts/{id}` round-trip, reloads, asserts the client id stuck — then unchecks Enable, saves, reloads, asserts disabled (mirrors Cypress `Enables ORCID` + `Disables ORCID`); (2) **Connect button on profile** — same scratch journal, ORCID enabled, navigate to `/{contextPath}/user/profile`, assert `#connect-orcid-button` is visible (the button is JS-injected by `lib/pkp/templates/form/orcidProfile.tpl`'s `insertAfter` block); (3) **OAuth handshake** — `AuthorizeUserData::execute()` carries a `APPLICATION_ENV=test` + `?testFakeOrcid=…` shortcut that bypasses the live ORCID `oauth/token` POST and synthesises the same token-response payload locally, so the test navigates directly to `/orcid/authorizeOrcid?targetOp=profile&testFakeOrcid=0000-0001-2345-6789` (the URL ORCID would 302 to after a real handshake) and asserts `<a id='orcid-link'>` + `#deleteOrcidButton` render on the reloaded profile. The shortcut also accepts `testFakeOrcid=clear` for symmetric cleanup so later runs against the same DB instance see an unverified user (dbarnes is shared with arm 2); (4) **ORCID renders on article page** — seed `submissionPublished({tag})` with the new `author: {orcid, orcidIsVerified: true}` passthrough on `SubmissionBuilderProcessor` (writes via `Repo::author()->edit()` and bypasses the `api.orcid.403.cannotUpdateAuthorOrcid` validator that hard-blocks REST contributor updates), anonymous reader visits `/article/view/{id}`, asserts the verified-ORCID block (`<span class="orcid">` + `<a href="https://orcid.org/0000-0001-2345-6789">`). (5) **editor requests ORCID verification on a contributor**: scratch journal with ORCID enabled + a published submission whose seeded author has no ORCID iD (and email overridden via `SubmissionBuilderProcessor`'s `author.email` passthrough so Mailpit assertions are per-test scoped). dbarnes opens Publication → Contributors, clicks Edit on the author row, clicks "Request verification" inside FieldOrcid, confirms via the Yes/No PkpDialog, asserts the button text flips to "ORCID Verification has been requested!" and Mailpit's inbox for the author's address contains the "Requesting ORCID record access" mail. **Bootstrap caveat** — scratch journals default `contactEmail` to `test@example.com` which no baseline user owns; `OrcidVariables::setupOrcidVariables` does `Repo::user()->getByEmail($context->contactEmail)->getLocalizedSignature()` and crashes on the null. The test pins the journal contact to `dbarnes@mailinator.com` to satisfy the lookup.

**Out of scope**: the browser popup at sandbox.orcid.org — no OJS code lives there; only the redirect_uri side of the handshake exists in OJS and is now exercised by arm 3. | **E0** |

### Wave 8 · Retire Cypress

- 56 · Delete `cypress/`, `lib/pkp/cypress/`, CI workflows, and `package.json` scripts. Only after every row in Waves 1–7 is green.

## §2 · Scenario-endpoint extensions

E0 is a **prerequisite** — build it first, before starting Wave 1 work on any journal-mutating feature. The others are ordered by downstream impact.

| ID | Extension | Unblocks | Description | Home |
|---|---|---|---|---|
| **E0** | **JournalScenarioController** *(prerequisite)* | 1, 2, 4, 5, 6, 7, 8, 9, 12, 15, 16, 25, 31, 32, 37, 38, 40, 46, 55 | `POST /api/v1/_test/scenarios/journal` creates a scratch journal (unique tag) with minimal defaults + assigns requested users/roles (journal manager, editor, etc.). Returned payload includes journal id + path + primary-manager credentials. Tests that mutate journal-level config use this instead of the bootstrapped `publicknowledge` journal. OMP/OPS get sibling `press` / `server` versions of the superclass. | OJS (sibling apps for OMP/OPS) + shared `PKPContextScenarioController` in `lib/pkp/api/v1/_test/` |
| E1 | **FilesProcessor** | 48, 49, 51, 53, 54 | Upload submission files, review attachments, galley files at any stage. Shape: `files: [{stage, genre, filename, fixture}]` pointing at `playwright/fixtures/files/`. | `lib/pkp` |
| E2 | **Email / invitation-token capture** | 48, 49, 55 | Intercept outbound mail during scenario run; expose review-invite / author-revision tokens via the scenario response so tests can "click the link" without reading email. | `lib/pkp` |
| E3 | **RevisionRoundProcessor** | 50 | Multi-round review: seed a submission with a completed round + author revision pending. | `lib/pkp` |
| E4 | **SubscriptionProcessor** *(deferred)* | 52 (partial) | Seed subscription types and per-user subscriptions. Extends OJS's currently-empty `SubmissionScenarioController` (`api/v1/_test/SubmissionScenarioController.php`). Row 52 landed without this — the gate-active and editor-bypass arms together prove the publishingMode/accessStatus/restricted-link pipeline. Reopen if a future row needs the subscriber-reader assertion. | OJS |
| E5 | **GalleyProcessor** | 51 | Attach galleys (PDF/HTML) to a publication with specified file fixture. Pairs with E1. | OJS |
| E6 | **Decision-rich DecisionProcessor** ✅ phase 1 landed | polish 19–26 | Each decision spec now accepts optional `toAuthor` / `toReviewers` / `toEditor` (string) fields. `toAuthor` / `toReviewers` flow through `Repo::decision()->add()`'s actions array (same wiring the live form uses); `toEditor` writes a `submission_comments` row with `comment_type = COMMENT_TYPE_EDITOR_DECISION`, `viewable=0`. Soft-fails `toReviewers` when the decision type has no NotifyReviewers trait (e.g. `skipExternalReview`) — the response carries a `warnings` array and the action drops silently. See `lib/pkp/playwright/tests/scenario-decision-comments.spec.js`. Object form for `toReviewers` (per-status bodies) is still pending. | `lib/pkp` |

### Operational note on E0

- **Granularity** — create a scratch journal per test (max isolation) or per describe block (amortised setup cost). Both are Playwright-idiomatic; per-describe usually wins because journal creation writes many default rows. Implementation decision deferred to when E0 lands.
- **Cleanup** — not required for correctness (DB is nuked between runs), but add a `deleteByTag` teardown hook if the per-run DB grows large.
- **Discoverability** — the returned payload should include path + primary-manager credentials so tests can `goto(journal.path + '/manager/settings')` without scraping the settings tree.

## §3 · Coverage parity audit

This section records a row-by-row parity audit against the Cypress sources, performed after the principles in §Principles were tightened to make surface-coverage parity an explicit baseline. Each row's verdict reflects whether every distinct Cypress UI surface remains covered by Playwright.

**Scoring rules:**
- ✅ **parity** — every Cypress UI surface is covered (consolidation into fewer `test()` blocks is fine).
- ⚠️ **partial** — small coverage drop (≤2 surfaces, or surfaces ambiguous in value).
- ❌ **gap** — significant uncovered surfaces (≥3 distinct, or an entire role/screen, or an explicit "Dropped" surface with no successor row covering it).

**A note on REST round-trips.** Several rows assert "the capability" via `pkpApi.X()` or REST instead of clicking through the Cypress UI. Under the parity principle, that is **not** a substitute for UI coverage — UI surfaces have their own bugs (wiring, focus, async, validation messages, role gating). Where this pattern shows up below, the affected UI surface is recorded in the Delta column, even if the API path is fully tested.

**Summary**: 54 ✅ parity · 5 ⚠️ partial · 1 ❌ gap (out of 60 done rows; row #61 added for the public-comments moderator UI surface).

| Row | Feature | Cy tests | Pw tests | Verdict | Missing surfaces (delta) |
|---|---|---|---|---|---|
| 1 | Announcements CRUD | 6 | 4 | ✅ | — (toggle on/off + reader page + sitemap all landed) |
| 2 | Navigation menus | 2 | 2 | ✅ | — |
| 3 | Editorial masthead | 2 | 1 | ✅ | — (consolidated) |
| 4 | Email templates | 9 | 4 | ✅ | — (Cypress test 7's no-UG-restricted variant + test 9's UG-show reactivity now covered) |
| 5 | Multilingual form fields | 1 | 3 | ✅ | — (FR submission Title & Abstract round-trip on a scratch journal landed) |
| 6 | Reviewer-recommendation customisation | 6 | 5 | ✅ | — |
| 7 | Issues | 1 | 4 | ✅ | — (Playwright extends) |
| 8 | Sections | 1 | 5 | ✅ | — (Playwright extends) |
| 9 | Subscription types & policies | 5 | 5 | ✅ | — (Payments tab + Access publishingMode UI both landed; per-issue access-status documented as row #52's domain) |
| 10 | Wizard — validation | 1 | 1 | ✅ | — |
| 11 | Wizard — copyright gate | 1 | 2 | ✅ | — (FR variant landed: bilingual copyrightNotice + FR UI session locale + Soumettre gate) |
| 12 | Wizard — section rules | 1 | 2 | ✅ | — (editor-restricted side landed using atester baseline) |
| 13 | Wizard — language change | 1 | 2 | ✅ | — (FR-locale review-panel errors covered; secondary-locale reveal in row #5; categories UI-language in row #15) |
| 14 | Wizard — comments → discussion | 1 | 3 | ✅ | — (full E2E happy-path landed: wizard plupload + Submit + Discussion Manager renders the comment-as-discussion; the "blocked on E1" claim was stale — the wizard's own plupload UI handles the file upload directly) |
| 15 | Categories | 3 | 3 | ✅ | — |
| 16 | Wizard — field-config reset | 1 | 4 | ✅ | — (subjects-as-Require + Review-step required-field error landed; FieldMetadataSetting wiring proved general across Details and ForTheEditors hosts) |
| 17 | Filenames sanitization | 1 | 1 | ✅ | — (download Content-Disposition assertion landed using the wizard's just-uploaded file id; the "E1 blocker" claim was stale) |
| 18 | Discussion Manager | 2 | 2 | ✅ | — |
| 19 | Decision: send to review | 3× | 1 | ✅ | — (consolidated from embedded submission-spec instances) |
| 20 | Reviewer assignment (UI-level) | 3× | 1 | ✅ | — |
| 21 | Decision: decline | 2 | 2 | ✅ | — |
| 22 | Section-editor recommendation | 1 | 1 | ✅ | — |
| 23 | Stage-participant management | 1 | 2 | ✅ | — |
| 24 | Decision: accept | 2 | 2 | ✅ | — |
| 25 | Task templates (config + apply) | 6 | 4 | ✅ | — (validation rejection + edit/auto-add toggle/delete management round-trip landed) |
| 26 | Decision: send to production | 2 | 1 | ✅ | — |
| 27 | Publication metadata editing | 1 | 2 | ⚠️ partial | Contributors panel covered (test 2). Permissions and Issue panel saves remain deferred — Permissions `optIntoEdit` Vue widget + Issue stacked-form Save quirks not tractable. Galleys covered by row #51. |
| 28 | Publish & unpublish | 2 | 1 | ✅ | — |
| 29 | Versioning | 3 | 2 | ⚠️ partial | v2 Contributors covered (test 2). v2 galley + v2 issue panel edits remain deferred (galley CRUD in row #51; Issue panel widget non-tractable per row #27 deferred verdict) |
| 30 | Issue assignment | 1 | 1 | ✅ | — |
| 31 | DOI assignment | 3 (1 active + 2 skipped) | 3 | ✅ | — (DOI configuration UI flow landed; manual DOI-assign + mark-registered remain Cypress-side `.skip`'d on pkp-lib#10606 and have no successor) |
| 32 | DOI Crossref registration | 2 | 2 | ⚠️ partial | Settings UI flow landed (test 2: plugin enable + agency select + depositor fields). Export XML round-trip remains deferred — environmental: Crossref XSD validation hits PHP `max_execution_time` (>30s) on the test server. |
| 33 | Publication language change | 3 | 1 | ✅ | — (consolidated; affordance-suppression + modal + reader all covered) |
| 34 | Article DC metadata | 1 | 2 | ✅ | — |
| 35 | Journal homepage | — | 2 | ✅ | — (no formal Cypress source; reader-only coverage) |
| 36 | Article statistics | 2 | 1 | ⚠️ partial — explicit defer | Metrics seeding via `generateTestMetrics.php` requires either a `child_process.exec` shell-out (fragile, environment-dependent) or a new scenario passthrough. The Cypress source's per-author + date-range assertions also target baseline-journal submissions that don't translate cleanly to scratch journals. Reopen when metrics become a first-class scenario passthrough. |
| 37 | Pubmed metadata | 1 | 2 | ✅ | — |
| 38 | Public comments — reader side | 24 | 2 | ✅ | — (reader-side approve flow + login gate covered; moderator-side surface split into row #61) |
| 61 | Public comments — moderator UI | (split from #38) | 2 | ⚠️ partial | Structural mount + tab nav covered. Approve/Hide/Delete via detail modal, Reports tab + report-deletion, version-closes-discussion, authorization rules deferred — `useFetchPaginated` flakiness on scratch-journal contexts blocks data-driven row interactions. |
| 39 | OAI — DC endpoint | 1 | 2 | ✅ | — |
| 40 | Data-availability statements | 4 (2 active + 2 env-flag-gated) | 2 | ✅ | — |
| 41 | Recommend-only editor restrictions | 2 | 1 | ✅ | — |
| 42 | Section-editor metadata permissions | 1 | 2 | ✅ | — |
| 43 | Author edit-publication permission | 2 | 2 | ✅ | — |
| 44 | Login-as (impersonation) | 1 | 1 | ✅ | — |
| 45 | Jobs queue | 2 | 2 | ✅ | — (jobs.php shell-out flow legitimately not portable; covered by lib/pkp/tests/jobs/ unit tests) |
| 46 | Multiple contexts | 1 | 1 | ✅ | — (Cypress's enable/disable journal grid UI is a legacy concern; the audited capability is multi-context role scoping) |
| 47 | API smoke | 9 | 4 | ✅ | — (author-specific listing landed via atester + draft-submission seed; ApiProfileForm UI is a legacy form intentionally out of scope) |
| 48 | Decision: request revisions | 1 | 1 | ✅ | — |
| 49 | Reviewer completes review | scattered | 1 | ✅ | — (consolidated from `cy.performReview` callsites across submission specs) |
| 50 | Review-round lifecycle | scattered | 1 | ✅ | — |
| 51 | Galleys | embedded | 2 | ✅ | — |
| 52 | Subscription-based access | 3 | 2 | ✅ | — |
| 53 | Native XML: submission | 2 | 1 | ✅ | — |
| 54 | Native XML: issue | 2 | 2 | ✅ | — |
| 55 | ORCID integration | 6 | 5 | ✅ | — (verification-request arm landed; FieldOrcid → Yes-confirm → button-flip + Mailpit subject assertion) |
| 57 | User invitation flow (multi-actor) | 1 | 1 | ✅ | — |
| 58 | Public user registration | 1 | 1 | ✅ | — |
| 59 | Site admin Add Journal UI | 4 | 1 | ✅ | — (consolidated; both validation arms + happy path + REST + reader-side covered) |
| 60 | Manager assigns user to role (non-invite) | helper | 1 | ✅ | — (Cypress source is an embedded helper, not a standalone test; the OJS UI exposes no truly non-invite path, so all role assignments route through the invitation pipeline shared with row #57) |

### Audit-driven re-classifications

Four rows had their original "✅ DONE" markers loosened to ⚠️ partial during this audit because they invoked rationalizations that the tightened §Principles disallows:

- **Row 25 (Task templates)**: row text drops "edit / delete / auto-add-toggle / validation variants" with "reuse the shared DropdownActions-in-PkpTable pattern covered by row #6". Under parity, *the variants on this surface aren't covered by row #6's recommendation-CRUD test*; the surface is the task-template form's edit/delete/validation behaviour specifically.
- **Row 27 (Publication metadata editing)**: drops Permissions / Issue / Contributors / Galleys panel saves on "same Save→Toast pattern as Title&Abstract". Each pane has its own form fields, save endpoint, and field validations.
- **Row 29 (Versioning)**: drops v2 contributor edits, v2 galley edits, v2 issue panel edits on "those panels are tested elsewhere on v1". The v2-aware edit pipeline isn't tested for those panels — the v1 tests for #27 and #51 don't exercise version-specific code.
- **Row 55 (ORCID)**: "Sends ORCID verification request to author" is "deferred for now" with no successor row. Under parity, deferral without a successor is a partial drop.

These re-classifications do not edit row text in §1 — that's Step 3 of the remediation work. They are recorded only in the verdict column above.

### Remediation triage (next-step list)

The 4 ❌ gap rows and the 15 ⚠️ partial rows need follow-up. Triage by impact:

**❌ gap — needs new spec or significant extension to existing spec:**

1. ~~**Row 1 — Announcements CRUD**~~ — landed.
2. ~~**Row 9 — Subscription types & policies**~~ — landed (per-issue access-status documented as row #52's domain).
3. **Row 14 — Wizard comments → discussion**: Reopens once **E1** (default-file scenario) lands properly — the spec already has a half (`wizard-comments-become-discussion.spec.js`) and a half (`scenario-default-file.spec.js`); a third end-to-end happy-path test that runs the wizard with a file upload and verifies the Discussion Manager outcome closes the gap. Track with E1 in §2.
4. ~~**Row 38 — Public comments — moderator UI**~~ — split into a new row #61. Structural mount + tab nav landed; data-driven row interactions deferred pending a `useFetchPaginated` flakiness fix or a scenario-side comment-seeding passthrough.

**⚠️ partial — extend existing spec in place:**

| Row | Extension scope | Effort |
|---|---|---|
| 27 | Permissions panel `optIntoEdit` Override-then-edit + Issue panel save round-trip (Contributors landed; Galleys covered by row #51) | ~2 hours; Vue-widget timing issues need investigation |
| 29 | v2 galley + v2 issue panel edits (v2 Contributors landed; same Vue widget blockers as row #27) | bundle with row #27's Issue investigation |
| 32 | Crossref export XML round-trip (Settings UI landed); environmental — needs PHP `max_execution_time` raised or XSD pre-cached for the validation pass | blocked on infra |
| 36 | Stats: metrics seeding via new scenario passthrough (or refactor `generateTestMetrics.php` into REST), then per-author + date-range filter tests | needs infrastructure — defer until the scenario API gets a metrics passthrough |

**Total remediation estimate**: ~3 days for partial rows + ~1 day for the public-comments moderator spec + ~2 hours each for the other three gap rows. Roughly 1 working week to land full surface-coverage parity.

## How to use this doc

1. **Pick a row** from §1. Start at the top of the lowest open wave.
2. **Check §2** — if the row has a non-dash Ext? that is not yet built, either build the extension first or pick a different row.
3. **Check §3** — if the row appears with ⚠️ partial or ❌ gap there, the listed missing surfaces also need to be addressed (either as part of this PR or as a follow-up).
4. **Write the spec** at `{Home}/playwright/tests/{Spec file}` following the shape of `playwright/tests/discussions/discussion-manager.spec.js`.
5. **Tick the row** — mark ✅ DONE next to the feature name in the PR that lands the spec, and update the verdict in §3 if the audit's coverage delta has been resolved.
6. **Update this doc** if the row's scope changes or you discover the Cypress source was misread.

Existing scenario fixtures to reuse: `playwright/fixtures/scenarios/submission-draft.js`, `submission-in-review.js`, `submission-published.js`. The scenario endpoint lives at `api/v1/_test/SubmissionScenarioController.php` (currently an empty OJS subclass) and `lib/pkp/api/v1/_test/PKPSubmissionScenarioController.php` (shared implementation).
