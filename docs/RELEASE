OJS 2.4.1 Release Notes
GIT tag: ojs-2_4_1-0
Release date: October 2, 2012
=============================

New Features
------------

Bug Fixes
---------
	#3922# Allow for opt-in/out status updates/notifications
	#6292# Form "name" element has been deprecated
	#7848# Both medra and datacite plugins include duplicate classes
	#7855# Cannot reindex with external helper programs
	#7856# ABNT plugin calls nonexistent getDOI function
	#7862# Native XML import doesn't respect article locale="..." attrib
	#7864# Unquoted field in query - filterStatus browsing subscriptions
	#7876# Submission editing DB syntax error
	#7882# Submission file downloads fail for editors in review stage
	#7883# User import/export calls nonexistent user insert get/set funcs
	#7931# Uploading a layout file for submissions generates a fatal error
	#7932# Dereference symlinks in build script
	#7933# viewing payments generates a fatal error
	#7936# AbntCitationPlugin.inc.php uses undefined constant String
	#7937# no or empty identify types shouldn't be considered in browse
	#7941# Link to article page fails to redirect to login
	#7943# Request parameter missing from some HelpHandler calls
	#7912# func_get_args should not be used as a parameter in PHP<5.3
