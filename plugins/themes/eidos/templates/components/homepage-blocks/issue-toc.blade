@if($issue)
    <x-homepage-blocks.default slug="issue-toc">
        <h2 class="sr-only">
            {{ __('journal.currentIssue') }}
        </h2>
        @includeIf('components/issue-toc', [
            'issue' => $issue,
            'headingStart' => 3,
            'showArticleGalleys' => $showArticleGalleysInToc,
            'showArticleCovers' => $showArticleCoversInToc,
        ])
    </x-homepage-blocks.default>
@endif
