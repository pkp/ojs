<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE data SYSTEM "../dtd/xmldata.dtd">

<!--
  * 2.1.1_update.xml
  *
  * Copyright (c) 2005-2006 The Public Knowledge Project
  * Distributed under the GNU GPL v2. For full terms see the file docs/COPYING.
  *
  * 2.1.1 database updates XML file.
  *
  * $Id$
  -->

<data>
	<!-- Bug #2114: Scheduled articles sent to Archive and returned to active get stuck. Unstick them. -->
	<sql>
		<query driver="mysql">
			UPDATE articles a, proof_assignments pa SET a.status = 2 WHERE a.article_id = pa.article_id AND a.status=1 AND pa.date_scheduling_queue IS NOT NULL
		</query>
		<query driver="postgres7">
			UPDATE articles SET status = 2 FROM articles a, proof_assignments pa WHERE a.article_id = pa.article_id AND a.status = 1 AND pa.date_scheduling_queue IS NOT NULL
		</query>
	</sql>
</data>
