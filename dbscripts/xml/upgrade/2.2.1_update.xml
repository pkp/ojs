<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE data SYSTEM "../dtd/xmldata.dtd">

<!--
  * 2.2.1_update.xml
  *
  * Copyright (c) 2003-2008 John Willinsky
  * Distributed under the GNU GPL v2. For full terms see the file docs/COPYING.
  *
  * 2.2.1 database updates XML file.
  *
  * $Id$
  -->
<data>
	<!-- Bug #3289: Allow for suppression of author infomation in ToC -->
	<sql>
		<!-- Syntax for MySQL. -->
		<query driver="mysql">
			ALTER TABLE sections ADD COLUMN hide_author TINYINT NOT NULL DEFAULT 0
		</query>
		<query driver="mysql">
			ALTER TABLE articles ADD COLUMN hide_author TINYINT NOT NULL DEFAULT 0
		</query>

		<!-- Syntax for PostgreSQL. -->
		<query driver="postgres7">
			ALTER TABLE sections ADD COLUMN hide_author SMALLINT NOT NULL DEFAULT 0
		</query>
		<query driver="postgres7">
			ALTER TABLE articles ADD COLUMN hide_author SMALLINT NOT NULL DEFAULT 0
		</query>
	</sql>

	<!-- Bug #3488: Add RSS feed for thesis abstracts -->
	<sql>
		<!-- Syntax for MySQL. -->
		<query driver="mysql">
			ALTER TABLE theses ADD COLUMN date_submitted DATETIME
		</query>
		<query driver="mysql">
			UPDATE theses SET date_submitted = '2008-01-01 00:00:01'
		</query>
		<query driver="mysql">
			ALTER TABLE theses CHANGE COLUMN date_submitted date_submitted DATETIME NOT NULL
		</query>

		<!-- Syntax for PostgreSQL. -->
		<query driver="postgres7">
			ALTER TABLE theses ADD COLUMN date_submitted TIMESTAMP
		</query>
		<query driver="postgres7">
			UPDATE theses SET date_submitted = '2008-01-01 00:00:01'
		</query>
		<query driver="postgres7">
			ALTER TABLE theses ALTER COLUMN date_submitted SET NOT NULL
		</query>
	</sql>

	<!-- Bug #3461: "Requiring Abstracts" changed to be more versatile -->
	<sql>
		<!-- Syntax for MySQL. -->
		<query driver="mysql">
			ALTER TABLE sections CHANGE COLUMN abstracts_disabled abstracts_not_required TINYINT NOT NULL DEFAULT 0
		</query>

		<!-- Syntax for PostgreSQL. -->
		<query driver="postgres7">
			ALTER TABLE sections RENAME COLUMN abstracts_disabled TO abstracts_not_required
		</query>
	</sql>

	<!-- Bug #3580: Add ability to indicate double issues -->
	<sql>
		<!-- Syntax for MySQL. -->
		<query driver="mysql">
			ALTER TABLE issues CHANGE COLUMN number number VARCHAR(10) 
		</query>

		<!-- Syntax for PostgreSQL. -->
		<query driver="postgres7">
			ALTER TABLE issues ADD COLUMN number_new VARCHAR(10)
		</query>
		<query driver="postgres7">
			UPDATE issues SET number_new = CAST(number AS VARCHAR)
		</query>
		<query driver="postgres7">
			ALTER TABLE issues DROP COLUMN number
		</query>
		<query driver="postgres7">
			ALTER TABLE issues RENAME COLUMN number_new TO number
		</query>
	</sql>

	<!-- Bug #3293: Enable user comments on a per section/article basis -->
	<sql>
		<!-- Syntax for MySQL. -->
		<query driver="mysql">
			ALTER TABLE sections ADD COLUMN disable_comments TINYINT NOT NULL DEFAULT 0
		</query>
		<query driver="mysql">
			ALTER TABLE articles ADD COLUMN comments_status TINYINT NOT NULL DEFAULT 0
		</query>

		<!-- Syntax for PostgreSQL. -->
		<query driver="postgres7">
			ALTER TABLE sections ADD COLUMN disable_comments SMALLINT NOT NULL DEFAULT 0
		</query>
		<query driver="postgres7">
			ALTER TABLE articles ADD COLUMN comments_status SMALLINT NOT NULL DEFAULT 0
		</query>
	</sql>
</data>
